# D31完整客户端188：系统组件、权限与配置核验

## 本批完成

完整客户端 `1.34.15-candidate`（188）沿原运行态诊断增加38项有限事实：七个应用包、十一个组件、十五个权限、默认短信应用、两个通知监听授权和两个电池优化豁免。仅按需查询，不增加启动或定时工作，不修改用户设置。读取异常逐项保留，不能以未开启替代未知。

电脑端复用既有远程任务、诊断和文件回传，核对采集前后的构建、启动身份、活动核心与报告摘要；本地保全回读后才清理服务器暂存。中断继续查询原任务，不重新投递。使用方式见 [产品配置核验说明](../../tools/D31-wireless-adb/source/tools/README-ProductConfiguration.md)。

## 验收结果

2056项组合检查中七项条件跳过，零失败和错误；双Lint、原签名及464份源码与测试副本核对通过。宿主八项、有限参考比较器十二项检查通过。

悉尼设备真实读取38项全部成功，原文件回传及保全清理闭环通过。四个取号服务入口保持禁用。有限参考比较为十五项匹配、一项原始差异、二十二项仍含未知预期字段；后者不是读取失败。唯一已知差异为 `RemoteManualReceiver` 的默认启用与显式启用，实际有效状态均启用，没有据此修改系统。

悉尼验收后推荐同一制品，两台获准大陆设备自然领取，三台活动核心及健康文件均为188、同一制品摘要，ROOT后台与云确认正常。大陆仅正常更新及只读健康核对，没有进行声音、媒体、切网、重启或ADB交互测试。

完整APK大小4912920字节，SHA-256为 `0163ddead4b14c87f6fe803cfe084a17ddbf249e95d7e5c67c9ef197703f9b1b`。基础构建为187，完整客户端与基础探针继续分离。

## 结论边界

运行权限不代表应用操作权限或业务通过；通知监听授权不代表服务已经绑定。初装参考与用户当前选择有区别，不生成或执行修复，不重放初始化，不证明全系统一致。仍需继续应用操作权限、辅助服务及具体配置消费者的原计划覆盖。

本批未重建正式固件1.4.4或Windows工具1.6.7，没有新增Web接口。后续打包仍需回填并复核新版客户端，不能把云更新视作固件已更新。
