# D31第九批核心接线与验收

## 2026-09-12 02:35 候选安装预登记

最后的异步Wi-Fi状态缺陷已修正，根桥与网络72项独立测试通过，交叉源码审查确认未观察目标时保留预留且不覆盖旧调用记录。统一构建目录为`research/d31/staging/parallel-batch9-build124/`，279份生成源码和测试与当前原件摘要一致；基础28项通过，完整911项中905项通过、6项跳过、零失败，双变体lint及身份检查通过。

目标为已确认的D31开发板，Android 6.0、API23、产品`hct6735_66_m0`，构建与本批只读基线一致。仅将完整122更新为124，不修改启动脚本、系统APK或分区，不重启整机。完整124为`1.26.0-candidate`、4674949字节，SHA-256为`a7fff4f9176fe99d3201205df33a09a064e296201feb75b473cea36cccd98f50`；基础123为135659字节，本轮不安装基础版。

命令采用`Install-RemoteFullCandidate.ps1 -Serial <D31有线完整序列号> -ApkPath <本轮完整APK> -ExpectedSha256 <上述摘要> -ExpectedVersion 124 -PriorSha256 1e9412893e412e9741512bd327b20ac87eb1207f54f06f07a1568f6d3288ab17 -CapturePath <b9-install124新目录>`。宿主固定电脑ADB端口5042，先保存原安装APK、系统APK、实际构建和进程状态；预期同包保留数据、手动更新状态成功、活动指针和特权身份吻合。未成功则保留现场，停止后续设备变更，使用真实备份及既有手动接替恢复流程处理，不以反复安装代替诊断。具体时间、设备序列号和原始输出仅保存在私有捕获目录。

## 范围与起点

### 2026-09-12 最终126设备验收与证据归档

- `b9-install126/`同包更新及独立核心自动接替通过，系统原APK保持。`b9-runtime126-before/`及`b9-runtime126-final/`确认实际核心ART、APP及安装摘要/版本一致，观察期间启动周期及APP进程不变，按需服务和媒体线程均已退出。不据此推断冷启动或长期稳定性。
- `b9-preserve126/`调用真实候选`preserve`通过：安装原件元数据和字节保持，归档根及摘要目录0700、APK0600、root属主、非链接，维护租约释放。已存在归档路径的这次真实调用不代替第二台设备首次迁移或异常掉电验收。
- `b9-network126/`严格通过：真实关闭Wi-Fi并进入等待确认，发起进程结束后不确认、不重发，独立守护到期恢复开启；事务及守护回执均为`ROLLED_BACK/restored=true`，原值真实回读、维护预留释放、同启动周期与候选APK通过。最终网络journal、调用回执及守护原件另保存在`b9-final-device-evidence/network/`。只证明本次有线管理条件下的单开关独立回退，不代表Web确认、跨启动恢复、所有网络参数或长期稳定性已通过。
- 124的通讯录与音频真实验收未因126重打而抹去或改写；126只改变目录与网络协调，统一组合包含原通讯录/媒体测试。最终没有新增持续媒体会话。
- `b9-runtime126-before/17-stdout-private.txt`记录QUIK的`ConstraintTrackingWorker cannot be cast to androidx.work.Worker`，与第5批已记录堆栈同型；本批未修改QUIK。最终1200条日志窗口指定匹配为0不撤销此前已观察到的异常，不能宣称整机零故障。
- `b9-final-device-evidence/fault-pending-private.txt`显示待归档容量32/32、事件索引错误0；分页返回16项，当前新事件接纳受上限限制。需要沿现有补传、验证和显式归档路径清理待处理名额，未删除原件，也不宣称本次新QUIK事件已自动完整冻结。其原始logcat已独立保存。
- `b9-cloud126-final/`生产GET已显示`1.26.1-candidate`及更新成功。没有重新POST，不以GET成功宣布会话503或浏览器故障归档已恢复。
- 993项设备原件、安装前像、两候选构建日志/测试报告/APK已登记长度、SHA-256及时间，共22980095字节；私有索引为`research/d31/staging/batch9-evidence-index-20260912/artifacts-private.json`。独立离线工作线证据另由各专项索引，本批不公开APK或私有原件。

### 2026-09-12 03:01 最终126安装与回归预登记

`parallel-batch9-build126/`构建及两种Lint通过，281份生成源码/测试与当前原件摘要一致。基础28项通过，完整925项中919通过、6跳过、零失败/错误，合计947通过。6项跳过为宿主符号链接相关条件未满足，不据此声明这些场景在Windows组合中已通过。独立网络77项与目录9项不重复加到组合总数。

完整126为`1.26.1-candidate`，4678312字节，SHA-256为`8bfeed74bfb6ad4b2f20840b7715e5334e21bf04eb467eed2c950e374161e9cb`；基础125为135658字节。仅同包安装完整126，采用既有安装宿主，前版本124摘要为`a7fff4f9176fe99d3201205df33a09a064e296201feb75b473cea36cccd98f50`，捕获`b9-install126/`。仍先备份安装原像及系统APK，不改启动入口、分区或重启整机；交接异常先保留现场再按原件恢复。

交接通过后依次核对实际运行身份，运行`RemoteReleasePreserveCheck 126 <完整摘要>`真实归档检查，以及同样20000毫秒的单Wi-Fi关闭/独立到期回退验收。后者新目录`b9-network126/`，仍保留有线ADB，不发送确认、不重发begin。归档检查helper使用126本轮class编译，摘要`6d66baf6118c98a44f07cf2e8dde96ee81782dbf0cb7d4def1221cfc12b8848d`；会调用真实preserve并核验权限，属于明确的维护诊断，不冒称纯只读。`Invoke-D31ReadCheck.ps1`新增操作说明参数仅用于如实记录该范围，不改变设备调用流程。预计只收紧归档目录/文件权限，安装原件字节、元数据及版本保持；具体前后原像存私有捕获，失败不重发或覆盖临时原件。

### 2026-09-12 02:49 网络第二次观察结果

仅修正`releases`父目录模式后，同124取得Wifi Binder前像、持久目录初始化、独立守护握手及真实关闭回读，进入`AWAITING_CONFIRM`。到期后Wifi读回开启、维护预留已释放，但事务及守护最终为`ORIGINAL_OBSERVED`、`rollback_attempted=true`、`rollback_returned=false`，严格宿主因此正确判失败。不能将此条写成完整独立回退验收成功。

`b9-network124-post-readonly/`已拉取整个本批网络目录，保留journal及两次调用回执；恢复调用的`setter_returned/request_accepted/target_observed`最终均为true。证据说明恢复Binder及目标观察落盘与父进程同步成功返回不是同一事实，尚需修正其协调合同；不能仅凭最终值补写父调用曾正常返回。后续修正保留未返回、未受理、未观察及原件缺失的不同语义，不用增加等待时间或伪造成功掩盖差异。当前实际Wifi原值和有线管理保持，未重启整机。

### 2026-09-12 02:46 网络首测前置拒绝与目录兼容修复

`b9-network124/`首测在只读Wifi前像命令返回`NETWORK_WIFI_CALL_UNKNOWN`后结束，`modificationAttempted=false`，没有prepare/begin或网络切换。宿主收尾保存进程、有线载波、同启动周期及活动APK证据。源码审查发现旧`RemoteUpdatePlatform.preserve()`仅递归mkdirs，未给`releases`父目录明确私有模式；真机`/data/local/d31-remote`为0700、`releases`为0777、候选子目录0700、APK0600。新`NetworkAndroidFiles.apk()`因此可能在Binder调用前被祖先校验拒绝，原通用异常输出不足以直接证明执行阶段。

最短验证仅把已确认为root拥有、非链接、位于既有私有父目录的`releases`改为0700，先保存完整原模式/属主/目录及APK摘要，其他内容不动，恢复原模式为0777。随后新目录`b9-network124-after-directory/`独立执行原宿主；若仍失败不能宣称已找到完整根因。开发源码同时修正版本目录创建模式和固定阶段错误码，后续新候选再做更新兼容验证，不放宽网络祖先校验。

### 已完成的真实业务检查

- `b9-contacts124-attempt2/`通过固定原厂服务的按需启动、APP Binder身份、一次LOCAL请求和真实END，共2条记录、2帧；不输出姓名号码，按需APP服务退出。这只证明选定来源应答完整，不证明Android联系人等价或原厂服务内部所有行为已理解。
- `b9-audio124/`通过2500毫秒本地诊断，实际读取73088字节、36544个单声道PCM采样，输入归属、停止、释放、工厂无残留及服务线程退出均满足合同；没有保存或上传声音。持续媒体及跨业务并发仍不据此开放。
- `b9-cloud124-readonly/`生产GET已显示`1.26.0-candidate`、更新成功，Nexui和QUIK已配置线路状态为新鲜注册；未配置线路明确未配置。没有管理POST或新会话操作，不能推断既有503已恢复。
- 交叉审查要求修正两个宿主异常收尾缺口：媒体服务退出断言失败不能跳过其它音频/身份采集；网络begin失败不能跳过原任务与守护回执的独立观察。宿主修正不改变124设备APK，不需要重新升级候选版本。

### 2026-09-12 02:41 网络单开关验收预登记

同一完整124开发板的当前ADB地址已确认属于`eth0`且载波为1，Wi-Fi设置读数为开启；正式宿主仍须通过Wifi Binder读取稳定前像，任何未知状态均拒绝启动。只执行一次`Verify-RemoteNetworkRollback.mjs <D31有线完整序列号> 124 <候选摘要> <b9-network124新目录>`，在保存前像、独立守护握手通过后将Wi-Fi切为前像反值，确认窗口20000毫秒。发起进程结束后宿主仅查询，不发送确认、不重发begin、不切有线接口。预期独立守护回到真实前像并记录`ROLLED_BACK`、`restored=true`及维护预留释放。

事务意图、前像、守护心跳和回退回执保存在机内独立私有目录，宿主归档原始命令及输出。失败时继续有界只读收尾，保留原失败；若不能自动恢复，则在当前有线通路按原任务取消/恢复，先读取未决调用记录，不以手动覆盖开关或删除预留掩盖未知调用。该测试不模拟整机重启，不证明跨启动守护可自动拉起，也不开放Web网络写入。

### 2026-09-12 02:38 通讯录首次拒绝及第二次尝试依据

`b9-contacts124/`保存首测失败回执`CONTACTS_MAINTENANCE_BUSY`，尚未取得维护租约，未进入原厂服务请求。宿主因成功字段缺失报告`CONTACTS_HOST_RECEIPT_SCOPE`，不是原厂返回空通讯录。随后`b9-maintenance124-readonly/`保存维护锁inode及六次内核锁表，连续观察期间维护锁均未占用、无修复预留，支持短暂竞争而非残留事务的解释，但不证明首测持锁者身份。

允许在这一新鲜释放证据下以同APK进行第二次独立尝试，输出到`b9-contacts124-attempt2/`。这不是新版本或自动重发，保留首测拒绝，不用第二次成功宣称稳定；若仍失败先定位合同或具体设备回执。

### 2026-09-12 02:37 同包交接结果及按需验收预登记

`b9-install124/`保存了完整安装前像、系统原APK、构建指纹和交接回执，124同包更新成功，系统原APK未替换。`b9-runtime124-before/`确认核心ART与APP实际摘要相符、按需服务为空、无媒体线程，同一观察期内启动周期与APP进程未变化，最近1200条日志指定故障匹配为0。

随后顺序执行`Verify-RemoteContactsRead.mjs <D31序列号> 124 <候选摘要> <b9-contacts124新目录>`与`Verify-RemoteAudioLifecycle.mjs <D31序列号> 124 <候选摘要> <b9-audio124新目录>`。通讯录只请求一次原厂`LOCAL/all_contacts`并仅输出计数元数据，预期真实END及解绑、专用服务退出；原厂服务启动内部副作用尚未证实，不强停原厂服务。音频只做2500毫秒本地诊断，不保存声音、不上传、不拨号，预期真实输入归属、停止及释放回执和线程退出。各宿主先保存真实构建/活动摘要及原始输出，失败不重发，先读证据再判断恢复；不改变业务账号。单次成功仅证明本次条件下通过。

用户要求继续核心开发，丢失模式和客户端界面仍延期。当前设备起点为完整122、`1.25.0-candidate`，已验证同包更新和独立核心自动接替；第八批源码提交为`fc5bf3afd83080f7f7032e2fd12962f539bbdc5b`。不重复第八批已经完成的验收，不因候选版本变化重新发布Windows工具或正式固件。

## 并行分工

1. 通讯录工作线负责原厂冻结代码中的服务启动生命周期、按需绑定、`LOCAL/all_contacts`有界读取及无个人内容的结果验证。第八批无原厂服务时的`flags=0`超时不原样重测。
2. 网络工作线负责单个`wifi_enabled`适配器、生产持久目录和独立回退守护。恢复条件未验证前保持`network_write=false`，不切断正在使用的管理网络。
3. 媒体工作线负责生命周期与实际驱动、可信观察、心跳和占用守卫的连接。未完成实际会话验收前保持`managed_media=false`及空模式集合。
4. 复核工作线检查最新Web故障归档修正状态，并独立审查通讯录并发、硬截止、清理和敏感内容边界。不修改共享Web产品代码。
5. 总负责人维护公共入口和互斥、审查集成、统一构建签名、安排D31设备操作、核对证据及提交开发源码。子任务不得并行操作设备。

## 验收与恢复边界

先完成离线代码和针对性测试，再冻结构建输入并生成新候选。设备变更前追加实际版本、摘要、具体命令、预期和恢复路径；保存真实安装前像，保留系统原APK。默认不重启整机、不修改启动脚本、不刷分区、不更改业务账号。通讯录不输出姓名号码，不把超时当空列表；网络原值观察与实际执行恢复分开报告；媒体模型与实际采音、网络会话分开验收。

当前生产管理POST及会话接口的503属于已有外部阻塞，GET成功不能代替其恢复证据。继续本地及ADB开发，所需Web修改在本批末尾追加到`remote.md`。首次普通基础版转系统完整版、完整固件实刷及双机比较仍等待另一台设备。

## 执行状态

已复用四位协作代理启动本批任务；此条仅登记范围，不表示代码、构建或设备验收已经通过。实际结果随后追加。

## 已读取基线

- `research/d31/captures/2026-09-12/b9-runtime122-before/`确认完整122实际核心ART映射、默认APP安装版本及摘要一致，观察期间启动周期和APP进程保持。按需服务为空，无媒体线程，最近1200条日志没有指定致命错误或ANR匹配。仅代表当前有限观察窗口。
- `research/d31/captures/2026-09-12/b9-cloud122-readonly/`的生产设备列表与发布列表GET均为200；目标当前版本为`1.25.0-candidate`，更新状态成功。未执行管理POST，不据此宣布会话故障已恢复。
- 集成审查要求网络恢复进程不能等待发起方持有的日志锁完成握手；平台调用也不能因同步Binder挂起永远占住恢复所需锁。本批独立恢复接线须覆盖这两条实际执行路径。

## 公共接线与独立审查

总负责人增加`RemoteNetworkAccess`，在原`repair.json`维护协议上标记网络事务归属，让现有系统监督无需替换即可识别预留。发起操作要求当前已验证核心，恢复操作不要求云端和核心健康；只接受同事务、同冻结APK的预留。没有添加新的常驻开机任务或修改系统APK。

独立审查发现并修正三项清理缺陷：无事务清理与同号重试间的竞态；终态维护锁暂忙导致守护退出；前像落盘前发起方死亡后缺少取消清理入口。现在统一由日志锁内重读后释放预留，锁忙进入原有有界重试，无日志取消明确`restored=false`。根桥与网络组合独立编译67项通过，证据为`research/d31/staging/batch9-network-root-integration-20260912-0227/`；这不是设备恢复验收。

另一个交叉审查问题仍在构建前处理：同步Wi-Fi调用返回不代表已接受的异步切换落实，未观察到目标时不能仅凭当前前像解除预留。必须区分请求返回、请求拒绝、目标实际观察及跨重启失效，不能用超时后读取一次旧值代替恢复。

通讯录采用工作线新增的`Verify-RemoteContactsRead.mjs`作为本批唯一LOCAL读取验收工具，旧`Verify-RemoteContactsBridge.mjs`仍只验收旧绑定合同。媒体使用新`Verify-RemoteAudioLifecycle.mjs`，额外核对输入归属、停止原因、工厂无残留和实际APP媒体线程退出。网络使用`Verify-RemoteNetworkRollback.mjs`，只在确认当前ADB地址属于有线接口后测试一个Wi-Fi开关；发起进程退出后只查询，不发送确认和第二次begin，要求独立守护实际回滚和原值回读。三个宿主共13项测试通过，全部为离线替身，不表示已进行设备操作。
