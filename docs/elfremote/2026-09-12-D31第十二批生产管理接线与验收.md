# D31第十二批生产管理接线与验收

## 2026-09-12 范围与分工

用户要求继续下一步，并明确允许直接委托D22_Web_Dev完成所需网页开发。上一批完整132为真机基线；本批不重复第十一批LOCAL诊断通道验收，不提前发布正式固件或Windows工具。

主线负责公共任务入口、网络事务生命周期、独立确认进程、组合构建、安装和联合验收。通讯录工作线负责严格参数及按需APP快照适配；网络工作线负责生产HTTPS确认和严格回包核验。D22_Web_Dev负责现有网页任务路由、通讯录列表及网络确认服务端，双方先冻结字段再接线。

## 待完成项目

1. LOCAL只读通讯录正式任务：建立、分页、关闭；每页最多32条、快照120秒不续期，错误不伪装为空列表，不隐式重采。来源不扩展到其他地址簿。
2. 原system_config任务中的Wi-Fi布尔开关：冻结原任务、APK及窗口，复用既有回退守护。独立确认不占用新的云任务槽、不持日志锁联网，失败不延长原期限。
3. 专用任务收尾：AWAITING_CONFIRM不等于成功；服务器允许不等于设备提交；持久终态及预留释放核对后才上报完成。未知状态继续保留原号。
4. 通讯录真实网页列表及网络确认、拒绝、超时恢复联合测试。保留有线时只能证明开关目标下管理链路可达，不声称流量已走Wi-Fi。
5. 故障保全刷新后续传及显式归档仍待真实目录授权验收，不能用本地目录替身冒充通过。

## 验收与回滚

代码和离线测试通过后再生成新候选，安装前记录APK摘要、设备构建、原活动核心及监督原像。只操作D31，电脑ADB端口5042；不碰H13或Pixel连接。不写boot/recovery、不清数据。候选失败使用保留的旧同签名APK及原活动路径恢复，网络测试由既有原值日志和独立守护回退。

网络生产写能力在联合验收前保持关闭。客户端界面、丢失模式、首次实刷及第二台设备比较仍按原计划延期。以下只追加实际结果，方案不计作已完成。

## 2026-09-12 20:54 候选安装预登记

最终制品为基础133及完整134，版本`1.29.0-candidate`。完整包4721572字节，SHA-256为`bad02b032548b5cc0ae245fceb17963777d7e1684da197bebf92fee034c65d2a`；基础包135653字节。目录`research/d31/staging/b12-variants133-134-final-20260912-2052/`。1166项通过、6项条件跳过，零失败；Lint、原签名及305项源码输入核对通过。前一临时制品没有安装，其原件保留。

交叉审核发现并修正：开始前失败悬挂、未开始拒绝结果、原值未改的状态映射、取消与开始竞态、未开始终态与重放标记的顺序、延迟领取到期结果缺失。最后两项已独立复核源码消除，最终组合测试包含新增过期和取消回归。

目标D31仍是API23及已验证构建，原活动完整132摘要`b7a7be5d228bf2c844f3a6c8d3c47ec855cb7005504de12fec38f68bc7cf4e73`。只读基线在`research/d31/captures/2026-09-12/b12-preinstall-2053/`，有线与Wi-Fi均已连接，Wi-Fi开关为1；维护预留不存在。

执行`Install-RemoteFullCandidate.ps1`，指定D31完整序列号、最终APK/摘要、版本134、原活动132摘要及新捕获目录；宿主先备份原安装与系统APK，再调用`adb -P 5042 -s <已核验D31序列号> install -r <最终APK>`。预期同包保留数据、实际核心及活动映射切换134，系统身份保持。失败保留现场，使用备份及已有原号更新恢复入口，不刷分区、不清数据。

## 2026-09-12 21:02 安装结果与网络联测预登记

完整134覆盖安装成功，独立核心自动接替；运行映射、摘要、版本与新鲜健康检查通过，最近1200条日志未匹配到目标故障。安装及运行证据分别保存在`b12-install134-2054/`与`b12-runtime134-2056/`，位于当日捕获目录。生产设备报告已显示通讯录分页及独立网络确认能力，普通`network_write`仍为关闭。固定系统监督保留132，已验证其恢复入口可以从原事务冻结APK加载134的恢复逻辑，无需替换系统监督。

本次只改变D31的Wi-Fi布尔开关，有线保持连接。宿主使用`Verify-RemoteNetworkConfirmationWeb.mjs`，固定完整134摘要及60秒确认窗口，先`--target false`，完整终态核验后另建明确反向任务`--target true`恢复原值。预期为设备原日志提交`CONFIRMED`且预留释放，而不是仅HTTP成功；失败先核对原任务和自动回退，不重复提交未知请求。宿主35项离线测试已通过。此测试不证明实际流量改走Wi-Fi。

## 2026-09-12 正式生产接口验收结果

| 项目 | 实际观察 | 证据目录（均位于当日私有捕获目录） |
| --- | --- | --- |
| 关闭Wi-Fi | 11次API请求、宿主约20秒；设备8243毫秒内提交`CONFIRMED`，实际开关为0 | `b12-network-off-2102/` |
| 恢复Wi-Fi | 新的明确反向任务，12次API请求、宿主约22秒；设备9904毫秒内提交`CONFIRMED`，实际开关恢复1 | `b12-network-on-2105/` |
| 通讯录 | 51次API请求，LOCAL两条分两页；复读一致、显式关闭成功，关闭后`409 CONTACTS_SNAPSHOT_GONE`且`not_enqueued=true` | `b12-contacts-formal-2107/` |
| 运行及退出 | 完整134摘要与实际APP/核心映射一致、健康新鲜、按需服务为空、无媒体线程；确认与回退临时进程为0、维护预留释放 | `b12-post-runtime134-2110/` |
| 最终云报告 | 设备及发布列表200，设备为`1.29.0-candidate`、更新成功 | `b12-cloud-final-2112/` |

网络变更与通讯录业务由真实生产任务API执行，不用ADB代替。ADB仅用于事后原日志采集及运行核对；网络始终保留有线，不将此次验收称为Wi-Fi承载切换。两任务原日志分别25帧、29帧，逐帧长度及SHA-256全部通过，原任务、APK、启动、前像、目标和期限与云终态完全匹配；本机持久确认随机号及签发时间也匹配，均早于原期限。只读复核程序为`research/d31/staging/Verify-B12NetworkEvidence.mjs`，每个捕获目录的`device-cloud-evidence-summary.json`保存结果。通讯录内容、设备标识和完整原件只留本地，不进入公开文档。

Web工作线已发布中间阶段兼容修正`3007426cfae045d8aa4641abf9e07112ab869db4`，Actions `34689827063`成功；`PREPARED/APPLYING/ROLLING_BACK`只能上报`running`及未完成清理，不被当作成功。正常路径通过后已直接请求撤回短期私有网络验收门，普通`network_write`始终关闭。

宿主通讯录独立运行44项通过、1项可选Web交叉校验默认跳过；启用真实Web合同的结果另补。网络最终宿主独立副本35项通过、8项可选Web测试跳过；有Web源码时43项通过。此计数与Android组合1166项分别记录，不重复相加为同一测试集。公开源码不再要求存在另一工作区的Web代码。

## 保留的验收边界与下一步

本轮CUA浏览器返回`Codex auth token is unavailable`，无法取得可操作页面。因此上述是实际API端到端验收，不是页面点击验收。故障保全的刷新目录恢复、显式归档，以及通讯录页面操作仍未完成；不能用宿主API或替身目录冒充。最近1200条日志未匹配目标故障仅代表该有限窗口，没有宣称整机长期稳定或通话验收。

收尾复核：通讯录170项文件包含111份状态快照及6份终态回执，摘要与绑定一致，未决步骤为0；已启用真实Web合同交叉校验，最终45通过、零失败、零跳过。Web工作线已删除临时网络验收门并从服务端配置清单确认不存在，私有撤回凭据为Web工作区`.wrangler/d31-b12-gate-1789210509975/revoked.json`。其当前另行工作为R2清理及文件取回后的删除回执，不将该工作误称为新增故障归档流程。

网络实际拒绝、失联、取消及跨启动的生产确认组合仍待独立验证；此前132本地跨启动原值恢复证据保持，不自动升级为本批生产确认验收。下一批继续现有单向媒体会话、停止与占用合同和系统配置语义对照；Web所需部分直接交接D22_Web_Dev。客户端UI、丢失模式、首次基础转系统完整实刷、第二台设备差异及真实修复仍按既定条件延期。本批只提交开发源码，正式Windows、固件及README下载地址不变。
