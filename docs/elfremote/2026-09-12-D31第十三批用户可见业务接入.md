# D31第十三批用户可见业务接入

## 范围与执行顺序

2026-09-12，用户要求继续剩余开发并授权直接委托D22_Web_Dev。沿上一条审计纠正优先级：定位、拍照与响铃、单向麦克风三条业务线并行，主线统一报告/会话接线、权限清单、构建和设备验收。先复用现有模块，不为已存在的服务端协议重复建系统。

基线为D31完整134，版本`1.29.0-candidate`，摘要`bad02b032548b5cc0ae245fceb17963777d7e1684da197bebf92fee034c65d2a`。下一组基础135及完整136使用新候选；构建后登记确切摘要再安装，不覆盖旧制品。本轮不操作其他设备，不写boot/recovery、不清用户数据。安装前使用现有宿主备份旧安装与活动版本，失败保留证据并走已有回退。

定位线完成应用身份内主动采样及无线观测，上报沿`radio`；GPS取点失败必须保留真实原因，位置采样不拖慢开机或核心健康。媒体线实现正式会话领取和单向音频；拍照/响铃线接入真实照片采集上传和播放停止。共享入口由主线负责，子任务不并发操作D31。

## 已知边界

本轮开始时浏览器控制仍返回`Codex auth token is unavailable`，不能完成自动页面操作。代码、协议、真机采集与API验收继续；最后需要实际页面试听/照片确认时再请用户操作，不能伪称自动浏览器已通过。

普通正式发布、UI、丢失模式和需要第二台设备的验收维持原安排。PTT、双向电话和录像沿本批会话基础继续，不能仅开放能力按钮冒充可用。本文件后续只追加真实结果。

## 夜间验收限制

2026-09-12晚，用户明确禁止测试响铃或警报。本批夜间不创建响铃会话、不进行扬声器播放测试；响铃代码仍进行离线检查，真机发声验收留到白天。拍照前也检查快门声，不能确认静音时暂缓实际拍照，只继续定位及无播放的业务验收。

## 定位设置变更预登记

2026-09-12晚，目标为已验证D31构建`hct6735_66_m0`、当前完整134。使用`tools/Enable-RemoteLocation.ps1`保存完整构建指纹、定位服务、权限及设置前像，再执行`settings put secure location_mode 3`及`location_providers_allowed +gps`、`+network`。预期是定位提供者启用且网络开关原值保持；不是GPS已取点的证明。设备通过固定本机端口5042及完整序列号定位，准确序列号和原始回执只保存在私有捕获中。

捕获目录为`research/d31/captures/2026-09-12/b13-location-enable-2301/`。若需回退，按其中`settings-before-private.json`恢复两个secure设置的原值；原值为`null`时使用`settings delete secure`删除对应键。不修改Wi-Fi、移动数据、账号、启动脚本或分区。先启用定位再安装136，以便新核心第一轮直接采样，避免额外重启核心。

## 组合构建及安装预登记

最终候选位于`research/d31/staging/b13-variants135-136-final-20260912-2303/`。完整136、`1.30.0-candidate`、4747803字节、SHA-256为`b0f9441e195ef33f3b63bdfde4ad17aa2b9b10e91bc7f44bcdc04cff1f9235d4`；基础135为135652字节。1216项测试中1210项通过、6项跳过，无失败或错误；Lint、双制品签名和327项生成输入来源校验通过。

较第一轮未安装制品增加`push-sync.media_session`兼容入口和MQTT同步优先处理，媒体续租独立于HTTP工作循环，并删除不必要的7秒ICE收集等待。旧报告邀请入口保留，各服务同号去重。网页生产修改由D22_Web_Dev负责，不能把客户端接线当作网页已上线。

安装使用`tools/Install-RemoteFullCandidate.ps1`，目标仅D31、当前完整134原像摘要`bad02b032548b5cc0ae245fceb17963777d7e1684da197bebf92fee034c65d2a`，捕获目录`research/d31/captures/2026-09-12/b13-install136-2306/`。先拉取安装APK与系统原件再同包覆盖，核对活动核心交接、系统特权身份和原系统APK未变。固定监督132、启动脚本及所有分区不改，不重启整机；失败停止新会话并按既有手动更新回退记录处理。

## 完整136真实验收与待修问题

同包安装、手动交接、活动核心、特权身份及系统原件不变均通过。`b13-cloud136-first/`的正式设备列表显示当前候选、就绪、有线承载、一个摄像头及三个已接入模式。首次位置为`cell`、精度290米；GPS真实应用UID的主动监听见`b13-business136-before/location-private.txt`，45秒窗口结束后监听移除，没有取得有效GPS坐标。

定位随后暴露有效期缺陷：`b13-cloud136-after-media/`回到`ip`、精度2000米。原因是无线原件120秒有效期同时用于报告，短于300秒采样周期，期间其它业务的新报告会缺少无线原件并回退IP。下一版分开“采集时缓存新鲜度”和“已冻结报告的有效期”，沿服务器15分钟上限，不改变真实采样时间、不加密集扫描。

静音照片真实会话通过，证据`b13-photo136-first/`：原会话编号上传并取回149072字节JPEG、摘要一致、自动关闭，`b13-business136-after-photo/`回读视觉服务清理完成且麦克风空闲。实际照片很暗，因此只确认采集/上传/回读链路，画面可用性留白天验证。快门必须成功禁声才允许拍摄，没有触发响铃。

单向麦克风首次未通过，证据`b13-microphone136-first/`及`b13-business136-microphone-live/`。已发布音轨、ICE已连接，APP原会话查询返回`MEDIA_TIMEOUT_OR_AUDIO_BUSY`、`capture_verified=false`及`cleanup_complete=true`。不能称为麦克风可用或浏览器已收到音频。当前根快照遗漏原会话原因、录音归属原始观察未保全；同时每帧调用静音API导致日志刷屏。下一版保留有界诊断、只在静音状态变化时调用平台接口，再定位实际失败门，不通过延长时间或绕过检查掩盖问题。

首个只读捕获脚本生成清单时误把正在创建的清单纳入哈希导致失败；原捕获未改，补充`artifacts-complete.json`保留包括空旧清单在内的原件索引。脚本已先计算完整列表再写索引，后续捕获正常完成。未将取证脚本错误描述为设备错误。

## 完整138安装与静默复核预登记

2026-09-12深夜，组合137/138已完成构建、签名、Lint及335项生成输入来源校验。完整138版本为`1.30.1-candidate`，4754388字节，SHA-256为`727ade66b7362aa523b65929bba8044c0eb12f39dab898a52c6564b6465cf36b`，原件位于`research/d31/staging/b13-variants137-138-20260912-2324/`。此次验证无线报告保留原采样时间、照片终态持久化及麦克风归属诊断；不改变录音保护窗口，不以RTP能量字段为零单独推断静音。

目标仍为前述已验证D31构建，安装前由`tools/Install-RemoteFullCandidate.ps1`重新核对型号、保存完整构建指纹、当前136安装APK及系统APK。前像摘要为`b0f9441e195ef33f3b63bdfde4ad17aa2b9b10e91bc7f44bcdc04cff1f9235d4`。捕获目录为`research/d31/captures/2026-09-12/b13-install138-2330/`，精确命令及完整设备序列号保存在私有宿主记录中。

预期安装后活动核心为138、同包数据和系统权限保留、系统APK不变。异常时保留现场并沿已有手动交接回退到备份136，不追加会话。固定监督132、启动脚本及boot/recovery不变，不重启整机。全程禁止响铃、警报、扬声器播放及诊断音；静音照片必须经过快门禁声硬门，麦克风只做无播放上行。

## 完整138复核结果

组合测试共1237项，其中1231项通过、6项跳过，无失败或错误。`b13-install138-2330/`证明同包交接及系统身份核验通过，系统原APK不变；没有重启整机。`b13-cloud138-first/`与`b13-cloud138-after-media/`均显示基站定位、精度290米，不能称为GPS定位。

`b13-business138-before/location-status-private.json`明确无线失败原因为`insufficient_fresh_access_points`，有效基站观测一项。不是简单没有采集代码，也不把有线承载伪称Wi-Fi定位。继续核查扫描原始数量和新鲜度，在取得证据前不猜测是权限或硬件故障。

`b13-photo138-first/`完成真实静音拍照、原会话上传及150092字节JPEG取回；摘要为`b459939202f94be5a63931180c0c0e1825a995cb8f10e17b78820075fb5ae1a9`。APP终态原件已拉取至该目录的`terminal/`，核心回读为`completed`、`closed=true`、`cleanup_complete=true`且`error`为空。此前空错误字符串被当成失败和终态被清空的问题已通过本次回归。没有验证白天画质或网页点击操作。

`b13-microphone138-first/rtc-diagnostics/`保存两个真实诊断原件，失败门为`INPUT_CONFIGURATION_UNVERIFIED`，核心明确报告`MEDIA_RTC_INPUT_UNPARSEABLE`。录音参数为48000赫兹、单声道、PCM16、输入源MIC，读取阶段完整、时间窗口有效；SFU音轨已发布且ICE连接成立。判断仅为配置解析不能确认，不能把它写成麦克风硬件坏或服务器无音频。已交音频子任务对照原始转储修复，保留3500毫秒保护窗口，不延长超时掩盖问题。

## 完整140采样率修复预登记

138两份原件确认输入线程、轨道和Policy均为48000赫兹；既有归属解析只接受此前实证的16000赫兹。140生产路径唯一行为变更为`setInputSampleRate(16000)`，不改变输出、音效、保护判门和时间窗口。独立PCM统计类及测试暂未接入生产回调。定位、视频未验收改动已保存独立暂存目录，定位五文件与138生成副本哈希完全一致，不混入本次验证。

组合139/140位于`research/d31/staging/b13-variants139-140-20260912-2341/`，版本`1.30.2-candidate`。完整APK为4755184字节，SHA-256为`ef391a21c9cf4abf4bac75ac34d865d36a950a615e88b3bb1c25255c26c5f4ff`。1241项测试中1234通过、7项跳过，零失败；新增私有原件回放在组合构建无输入时跳过，其独立47项回放有真实证据，见麦克风记录。337份生成输入、Lint及签名检查通过。

仅向前述D31安装，捕获目录`research/d31/captures/2026-09-12/b13-install140-2345/`。宿主先备份安装138及系统原APK，前像SHA-256为`727ade66b7362aa523b65929bba8044c0eb12f39dab898a52c6564b6465cf36b`，随后同包安装、核验活动核心及特权身份。系统原件、监督132与启动脚本不变，不重启整机；失败保留原件并沿手动交接回退。验收要求真实AudioRecord与两源输入均为16000赫兹、自身归属成立、会话就绪及停止释放；浏览器另做不连接播放输出的解码PCM统计。禁止任何设备或电脑发声。

## 完整140真实麦克风与联合定位结果

同包升级与系统身份核验通过。`b13-microphone140-first/summary.json`记录正式会话就绪、音轨发布、停止确认及服务端释放均通过。其私有诊断两份原件中的Flinger与Policy均为16000赫兹，实际AudioRecord同为16000赫兹，`SELF_ONLY`、`external_state=IDLE`、`input_verified=true`；停止后`cleanup_complete=true`。因此此前48k默认输入与16k检查不匹配已被本次单变量实测证实。停止后的`capture_verified=false`表示已不再采集，不能据此否定会话期间验证。

`b13-cloud140-first/`显示`loc.source=network`、精度18米；设备`b13-business140-browser/location-status-private.json`含六项有效Wi-Fi观测、一项小区，GPS仍为`timeout`。140保留138定位实现，没有延长两秒扫描窗口。因此不能把早先AP不足推断成Wi-Fi定位永久不可用，也没有依据把六秒扫描等待当必需修复；后续先补诊断数量及扫描结果。

浏览器静默PCM验收已委托有正常浏览器的D22_Web_Dev，由其独占一次D31媒体会话，禁止本地输入及播放输出。对方因自身服务端释放核验请求超时暂未开始；本机保持140，不以此宣称D31网页端验证完成，也不与其并发安装或开启其他会话。

## 视频接线范围

下一候选复用相同APP服务、会话租约、音频归属及上行协议，新增按需EGL和Camera1视频轨道。就绪同时要求录音归属、视频首帧、发布和ICE；视频未释放时保留原生依赖并报告未知释放。计量或未知网络使用较低视频档位。没有开机打开摄像头、创建本地录像或新增上传队列，网页继续承担既有录制与回放。

PCM统计只记录处理后的累计计数和幅度，不保存声音、不参与授权；为避免每次计数变化造成报告循环，核心通知只比较会话及能力状态，诊断仍保存在私有快照。PTT与双向通话继续独立暂存开发，当前不开放能力，不执行播放验收。

## 完整142组合及安装预登记

组合141/142位于`research/d31/staging/b14-variants141-142-20260912-2358/`，版本`1.31.0-candidate`。完整APK为4763785字节，SHA-256为`252f79b7f228fdf286625dbb568ce3d729e0d4356e9b11e4ed73cae66170db4c`；基础141为135652字节。1276项组合测试中1269通过、7项条件跳过，零失败或错误；345份生成输入、Lint及签名核验通过。

142新增视频上行生产接线、只读PCM统计、明确Wi-Fi扫描诊断；继续保留原两秒扫描等待及位置更新周期。交叉审核已修复视频回调与会话监视器之间的锁顺序问题、停止录音异常跳过相机关闭、原生对象释放失败后继续销毁依赖，以及具体启动/清理原因丢失。生产状态和诊断变化分别处理，不因帧计数推进频繁唤醒云报告。

须在Web工作线明确结束140静默会话后才可安装，目标仅既有D31。预定捕获目录为`research/d31/captures/2026-09-13/b14-install142-0005/`。现有宿主先备份安装140及系统原APK，前像摘要为`ef391a21c9cf4abf4bac75ac34d865d36a950a615e88b3bb1c25255c26c5f4ff`，再同包安装并核对核心交接、特权身份及系统原件不变。监督132、启动脚本与各分区不改，不重启整机；异常保存现场并沿既有手动升级恢复，不继续开启新媒体会话。预期视频取得本地首帧、两条上行轨道、明确停止及资源释放；实际网页画面及录制回放另行核验，不以API发布成功替代。夜间发声禁令继续有效。

## 2026-09-13 完整142首轮视频退出缺陷

140浏览器静默验收已结束，设备端确认原会话关闭并清理后，142完成同包更新及系统身份验证，没有重启整机。`b14-video142-first/`仅证明服务器会话、音视频轨道发布及停止应答通过；不等于设备资源释放或浏览器画面通过。宿主汇总已明确加入`scope=SERVER_SESSION_AND_TRACKS_ONLY`、`deviceCleanupVerified=false`及实际媒体内容未验收字段，避免把API成功扩大为整条业务通过。

设备回读`b14-video142-live/`及`b14-video142-after/`显示真实首帧640×480、PCM累计39200帧、峰值约0.00946、均方根约0.00207，随后`MEDIA_VIDEO_RELEASE_UNCONFIRMED`。相机系统回读为已关闭、无活动客户；完整日志证明AudioRecord停止、Camera1停止及纹理释放代码曾执行。原生依赖仍保留、媒体能力自动撤回，没有伪报`cleanup_complete`。

已撤回此前向Web发送的142就绪通知。Web工作线明确确认未创建142任何会话，故本轮只有主线这一视频会话。先修退出问题，再继续静默麦克风/视频网页验收。所有原件及失败日志保留，不把早期通用退出错误当作已确认的相机硬件问题。

## 2026-09-13 完整144退出修复与组合检查

固定WebRTC字节码确认主线发布时的`getTransceivers()`会释放原包装及其sender，142不能跨此调用保存sender引用。144仅保存稳定ID，在退出时一次性取得当前sender并移除；逐阶段保留失败原因，不放宽资源释放确认。详细证据见视频上行业务文档。

独立23项通过后，首次组合目录`research/d31/staging/b14-variants143-144-20260913-0014/`在测试编译失败：Android测试编译路径不提供直接导入的`sun.misc.Unsafe`。原件保留；仅将测试夹具改为反射取得桌面JDK分配器，生产实现及断言不变。重新构建使用全新目录`b14-variants143-144-final-20260913-0017/`，不能以独立测试替代组合通过。

安装前故障现场保全目录为`research/d31/captures/2026-09-13/b14-before144-0018/`，包含142媒体状态、录音及相机转储、活动服务、定位状态、构建身份和日志原件。现场仍为`release_unconfirmed`，未删锁、未重启整机、未创建新媒体会话。夜间禁止响铃、警报、诊断音及可听播放继续有效，网页测试须预先固定零增益且视频静音。

### 完整144安装预登记

组合1284项中1277通过、7项条件跳过，零失败或错误；346份生成输入逐项一致，Lint、版本及签名核验通过。完整144为`1.31.1-candidate`、4764885字节，SHA-256为`89e868cee6d88bc2459d5e8be7caf353b24c3c9aea386507ff0a2d2159814ca6`。基础143为135658字节。

仅向既有D31构建`hct6735_66_m0`执行`tools/Install-RemoteFullCandidate.ps1`；捕获目录`research/d31/captures/2026-09-13/b14-install144-0020/`，宿主记录完整序列号、构建指纹、时间及精确命令。先备份142安装APK和系统原APK，142前像摘要为`252f79b7f228fdf286625dbb568ce3d729e0d4356e9b11e4ed73cae66170db4c`。随后同包安装并核验144核心交接、特权身份和系统原件不变。固定监督132、启动脚本及各分区保持，不重启整机。

待安装和闲置状态核验后，仅创建一次静音视频上行会话；要求首帧、上行轨道、停止及设备`cleanup_complete=true`，相机及录音停止，媒体槽释放。若仍未确认释放则保存现场、停止新媒体会话，沿既有手动交接恢复到已保存制品；不通过删除互斥锁或扩大超时伪造通过。浏览器实际内容与本次API及设备释放验证分别记录。

### 完整144实际退出结果

同包安装、活动核心交接及特权身份检查通过，系统原APK未变。`b14-idle144-0021/`确认安装后媒体闲置、清理完成；生产列表已显示`1.31.1-candidate`。唯一静音video会话证据为`b14-video144-first/`，确认两条轨道及服务器停止释放；设备随后原件为`b14-video144-live/`和`b14-video144-after/`。目录名中的`live`不代表采集时会话仍在运行，该次回读时已经关闭。

真实视频首帧640×480，录音采样16000赫兹，PCM累计38880帧、峰值约0.01529、均方根约0.00228。最终会话与视频均`closed`、`cleanup_complete=true`，日志确认录音及相机停止、纹理释放；相机无活动客户，两个输出轨道表均活动数为零，媒体能力恢复可用。144通过本次退出修复验收，不据此宣称长期稳定或浏览器收到内容。

已将媒体控制明确交给D22_Web_Dev，先固定零增益麦克风解码，再经主线清理复核后单独静音视频。其持有控制期间主线仅抓只读原件，不并发安装或创建会话。

### 完整144浏览器静默结果及声音边界

麦克风窗口为悉尼00:23:13.798至00:23:23.339，3.538秒就绪。网页真实只接收音轨，预先将增益设为零再连接输出；六秒分析得到585728个重叠窗口采样值，全部为零，不能把这个数量当作独立音频采样帧。RTP为300包、19936字节，`totalSamplesReceived=0`。设备同号终态累计103200帧、峰值约0.01218、均方根约0.00273；`b14-browser144-mic-sample/`确认停止及清理完成。声音仍未验收，没有因RTP成功宣布声音可用。

随后独立视频窗口为00:25:15.794至00:25:26.187，首帧7.892秒，实际640×480、解码36帧，元素始终静音。服务器释放、浏览器连接及音频上下文关闭均通过；设备`b14-browser144-video-start/`及`b14-browser144-video-after/`再次确认`closed`和完整清理。网页原件分别在Web工作树私有目录`.wrangler/d31-144-silent-browser.txt`及`d31-144-muted-video.txt`，相关脚本同名；不公开会话原始标识或图像内容。

对照发现有静音视频元素消费流时音频`totalSamplesReceived=121440`，仅WebAudio图时为零。因此继续检查浏览器消费与实际PCM，不能直接认定D31编码静音。主线交接的后续检查依旧要求元素在绑定前设置`muted=true`、`defaultMuted=true`、`volume=0`，音频图增益先设零，不得为了取证解除静音。视频画面方向、白天画质、音画同步及录制回放仍是独立未验收项。

### 完整144静默PCM单变量复核通过

先做浏览器离线零增益控制图，确认音频上下文时钟推进且分析器取得非零数据。随后保持同一144设备实现，仅增加预先静音的音频元素消费真实远端流。窗口为00:28:53.193至00:29:02.903，3.688秒就绪；元素绑定前后均`muted=true`、`defaultMuted=true`、`volume=0`，分析图增益始终零，没有听音或诊断音。

浏览器上下文时钟从3.6907推进至9.7013秒；587776个窗口采样值中560140个非零，峰值约0.004639、均方根约0.0005123。真实远端音轨启用且存活，Opus、48000赫兹、双声道，RTP297包、20011字节，实际接收采样285120。因此本次麦克风上行与浏览器实际解码通过，此前仅WebAudio图的零值是该浏览器消费方式造成的假阴性，不修改Android编码、音量或保护逻辑来迎合错误测试。

服务器确认无会话、浏览器连接及音频上下文关闭，设备`b14-browser144-muted-audio/`同号累计103520帧且`closed`、`cleanup_complete=true`。Web原始8份脚本和结果已保全于`b14-browser144-web-acceptance-20260913_0030/`，附逐文件摘要清单。生产网页已有媒体元素消费路径，无需为本次测试新增接口。真实听感、回声及双向通话不属于静默PCM验收。Web明确交还控制并关闭测试浏览器，主线才继续定位候选。

## 2026-09-13 定位146组合范围

首次Wi-Fi缓存补读候选经主线与独立交叉审核后，发现并修正成功补读可能清空仍有效旧cell的问题。最终93项专项通过，24份候选输入与测试冻结一致；新增旧cell120秒准入、新cell优先、合并采用更早真实采样时间、时钟异常拒绝旧cell等回归。六份实现及测试经`apply_patch`合入，实际Sampler及新增测试摘要与冻结一致。

组合145/146只增加这组定位改动，媒体144代码保持。采用全新目录`research/d31/staging/b14-variants145-146-20260913-0031/`，版本`1.31.2-candidate`。补读复用原APP身份桥，不重发扫描、不改变网络开关、不延长四秒桥预算，也不在主线程等待。GPS没有有效坐标仍明确报告缺失，不能把Wi-Fi求点命名为GPS。详细设计和93项回归见`2026-09-13-D31首次WiFi扫描纯缓存补读候选.md`。

### 完整146安装预登记

组合1299项中1292项通过、7项条件跳过，零失败或错误；347份生成输入、Lint及双制品签名检查通过。完整146为4765773字节，SHA-256为`a738122686d65ab376fe83e476fe9f503276089913df926ed8977f3a9648ab6b`；基础145为135656字节。视频释放实现与已验收144相同，未混入PTT候选。

仅向已验证D31执行`tools/Install-RemoteFullCandidate.ps1`；捕获目录`research/d31/captures/2026-09-13/b14-install146-0035/`。安装前备份144安装APK、系统原APK及构建身份，144前像摘要为`89e868cee6d88bc2459d5e8be7caf353b24c3c9aea386507ff0a2d2159814ca6`。预期手动核心交接到146、系统身份保留、首次cell可先上报、GPS窗口结束后补读已有Wi-Fi缓存并退出服务。不更改任何网络开关、监督132、启动脚本或分区，不重启整机；安装或交接异常沿已保存144回退路径处理，不覆盖旧原件。

退出宿主`Verify-RemoteRuntime.ps1`补充对本批新RTC、视频、视觉及照片线程前缀的匹配，适应Linux线程名截断；此前仅检查三个老前缀可能漏报残留线程。此为电脑端只读检查，不修改APK或机内服务。安装后待单轮定位完成，再检查实际核心映射、健康时效、按需服务及线程退出。

### 完整146定位与运行态实测结果

同包更新及系统身份核验通过。`b14-location146-first/`显示首次实际扫描等待2006毫秒仍无新鲜AP，8条旧缓存全部过期，但一项cell已上报；`b14-cloud146-first/`为基站定位、精度290米，证明后台GPS及后续补读没有阻塞首份可用报告。

同轮结束后`b14-location146-cache/`显示`radio_cache_recheck.result=completed`、`wifi_scan_result=passive_cache`、扫描等待零；读到10条缓存、8项合规AP，上报6AP和1cell。最新扫描缓存年龄约43.2秒，保留原始采样时间，不把补读时间当新扫描。`b14-cloud146-cache/`显示网络定位、精度18米。GPS仍为`timeout`、无有效坐标，本结果不称为GPS成功。

`b14-runtime146-final/`完成真实运行态核对：146的活动ART映射与APP映射、安装摘要、版本及健康时效均匹配；顺序读取期间boot、核心PID、APP PID及安装路径一致。按需服务为空、扩充后的媒体线程检查无残留，最近1200条日志中目标异常匹配为零。此为本次有线承载顺序观察，不替代长期稳定、离线重启、全部电话功能或第二台机器验收。

本次146更新未重启设备，固定系统监督仍132，系统原APK、启动脚本、网络开关、个人配置及分区不变。无需再创建媒体会话以重复已通过的144代码验证。公开交付只同步开发分支源码和中文记录，PTT独立源码置于`experiments/ptt-downlink`并明确不编入完整146；正式Windows、固件、APK下载地址和R2发布通道不随本批候选自动改变。
