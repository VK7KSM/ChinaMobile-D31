# D31第七批并行开发与验收

## 目标与分工

依用户最新要求，丢失模式暂停到客户端UI阶段。本批只做核心业务，承接已验收的完整114及开发提交`f53877a9aa026db9d86785860cdb37fdd43b4f01`。

| 工作线 | 本批独立范围 |
| --- | --- |
| 音频归属 | 核对API23原生音频输入归属证据，独立解析与判断；未知不冒充自身 |
| APP媒体 | 有界采音诊断与退出，声音内容不落盘、不联网、不占电话模式 |
| 原厂通讯录 | 冻结源码查证完整列表合同、实现有界只读接口和离线测试 |
| 故障补传 | 轻量分页与详细验证衔接、旧队列持久恢复及测试 |
| 总集成 | 公共入口、设备证据、签名构建、同包安装、生产Web验收、文档和提交 |

## 验收约束

- 唯一目标为D31的API23，沿用专用5042服务器及明确完整序列号；不操作H13、Pixel或D22。
- 不刷分区、不清数据、不切网、不重启整机。原系统组件和启动入口保持；候选安装前备份已装114及系统原件。
- 采音诊断仅按需运行，设置硬性时间上限，采样内容丢弃，仅保存状态、字节计数和系统元数据。不拨号、不发短信，不开启整套媒体能力。
- 设备原像、原始日志和失败证据保存于全新捕获目录；公开文档只包含脱敏结论。未知归属、截断和未验证能力必须保留。
- 客户端UI与丢失模式不属本批。共享Web只读；精确对接需求最后追加到`remote.md`。

## 初始状态

已读取主计划第12.12节、最新共享交接及第五/六批专项。完整114的准备和退出已通过；真实采音、输入客户归属、原厂全量通讯录及轻量补传衔接尚未验收。本条是开工记录。

## 故障队列先行预登记

新宿主只依赖114已验收的`pending/query/export/get_file/archive`合同，因此在等待音频组合构建期间，先在现有114完成有限生产验收，不把证据版本写为未来116。

使用`FaultTransferCli.mjs`，目标绑定当前D31名称、`1.23.2-candidate`及摘要`46106ff0d892e155a2b9f5dbb85151226cd09d63f4e6d3098c9a276100f03993`对应活动APK；会话与实际设备身份仅在私有记录。全新状态目录为`research/d31/captures/2026-09-12/b7q114/state`。首轮限4请求、30秒和1事件；第二轮保持同目录与版本，限1事件、4候选、2页、96请求、180秒及8MiB。

预期首轮预算退出而非失败重发，次轮同号查询继续完成1个包的取回、电脑验包和显式归档。归档保留设备原件、不清磁盘；若该事件已归档，本次只补足新宿主本地保全证明，不能宣称再次释放一个新槽位。失败保留原任务与原件，不改业务设置、不重启、不自动删除；原110队列证据保持不动。

## 文件句柄耗尽与验证预登记

2026-09-12，第七批114队列首轮在目标解析阶段退出，未下发设备命令。Web工作线只读确认原设备身份和历史任务仍在，未配对设备因报告逾期被列表隐藏，不能据此重新配对。

设备独立核心仍报告本地可用，但上报阶段返回`report:SocketException`。同一进程实测1021个描述符，软限制1024；大量重复句柄指向故障扫描源和`boot_id`。私有完整原件保存在`research/d31/captures/2026-09-12/parallel-batch7-cloud-missing114/`。目前推断为API23借用式文件流未关闭`Os.open`原始描述符，需旧版与修复版同进程循环对照后才能确认。

独立`FileDescriptorLifetimeCheck`仅针对已核验D31 API23，以64轮读取系统文件、创建16字节合成样本、回读校验，记录每16轮的自身描述符数量。样本仅写入随机唯一的`/data/local/tmp/d31-fd-check-<随机标识>`新目录，不修改已有文件；进程退出释放自身资源，样本保留作证据。宿主沿用`Invoke-D31ReadCheck.ps1`，参数为随机标识，实际设备序列号、构建指纹、命令和JAR摘要保存私有原件。旧114和修复候选必须使用同一JAR、不同新目录，不重启核心来代替修复证明。

首个完整116组合因采音构造处缺少权限异常边界而未通过Lint；该失败保留，未安装设备。修复后的下一组合为基础117、完整118，名称`1.24.1-candidate`。候选安装继续沿用备份、签名和同包手动交接合同；先完成离线构建与专项对照，不改系统原件、启动入口或设备身份。

### 旧版文件操作对照结果与故障采集对照

旧114在同一独立进程64轮真实文件操作后，`fd_before=15`、`fd_after=207`、`fd_delta=192`，四个采样点为`63,111,159,207`。每轮两次读和一次创建，恰好每次打开遗留一个描述符；内容校验通过、已有文件未修改。证据为`research/d31/captures/2026-09-12/b7-fd-file114/`，JAR摘要为`546470f24588f74e700424f4c8fe6cbf68c4cc40640a5227c32a94a66c8c9002`。测试返回正常仅说明测量完成，不能把它视为无泄漏通过。

补充故障采集单变量对照：`DescriptorOwnershipCheck 64`在独立进程中通过实际`AndroidCollectionAccess`反复读取固定`boot_id`，不输出内容或摘要、不写设备数据，记录自身总FD及该源的残留FD。JAR摘要为`1b4910e94be03598724cb35113352d093a012cdb3d80608ede446849385cdeeb`。旧114预期非零退出并保留完整输出，修复候选须同JAR证明每轮关闭后数量不增长；不能只靠GC或进程退出回收。

### 完整118安装预登记

组合构建`research/d31/staging/parallel-batch7-build118/`已完成，742项执行、736通过、6项跳过、0失败/错误；两种Lint、源码副本哈希、签名及制品隔离检查通过。基础117为135653字节；完整118为4628195字节，SHA-256为`0805f2cdfa4ed16348b822cca83e1a3973d69d81ade763adc1b9fef1bbbc104a`，版本`1.24.1-candidate`。

本次调用`Install-RemoteFullCandidate.ps1`，明确指定D31专用5042服务器及完整序列号，前置核对型号、114活动摘要、原安装及系统APK备份、维护互斥；具体命令、指纹与原始输出保存在`research/d31/captures/2026-09-12/b7-install118/`。唯一安装动作是`adb -P 5042 -s <已核验D31序列号> install -r <本轮完整APK>`。预期通过手动APK更新接收器自动切换独立核心，安装与活动118一致且系统原件不变。失败先保留事务及原件，通过原有版本交接恢复旧载荷；不清身份、不用刷分区或整机重启掩盖失败。

设备通过后依次以旧版同一helper验证文件操作64轮和故障读取64轮，再观察同一核心连续扫描；其后单次2500毫秒APP采音诊断、原厂通讯录LOCAL元数据、轻量故障队列及生产Web回归。声音不落盘、不联网；原厂联系人值不输出。持续媒体集合保持空，丢失模式及UI保持延期。

## 完整118已取得的设备与Web结果

- `b7-install118`确认同包安装、手动更新事务成功、实际活动118和系统特权身份一致，原系统APK摘要未变，没有重启整机。
- `b7-fd-file118`使用旧版同一JAR，64轮后`fd_before=12/fd_after=12/fd_delta=0`，四个采样点均为12，内容核对通过。`b7-fd-collection118`同样使用旧版同一JAR，64轮`fdBefore=fdAfter=fdPeakAfterClose=12`、该源残留FD均为0、退出0。与旧版线性增长对照已通过，持续核心观察单独记录。
- `b7-sip118`确认原D31身份和名称重新上报`1.24.1-candidate`，已配置Nexui和QUIK线路新鲜注册。未重新配对、创建身份或修改服务器数据。
- `b7-audio118`经真实Web任务完成2500毫秒APP采音，读取76800字节、38400帧；停止、释放、工作线程结束均确认，声音不落盘、不联网。10组两源系统元数据完整，其中2组落入诊断时间范围；该范围含开始和释放，不自动证明全程独占输入。
- `b7-runtime118`确认核心ART及实际安装APP映射和摘要，APP进程、启动周期及安装路径一致；按需服务已退出，无媒体线程残留。最近1200条日志没有匹配的指定致命异常或无响应；范围不等于整机长期或冷启动验收。
- `b7q118/state`首次4请求预算停止，第二轮从同一状态和原任务继续，42请求完成1个7514字节故障包的导出、取回、电脑逐项验包、显式归档及独立查询，缓存15个后续候选。源窗口有缺口，原件未删；任务预算及原始输出分别在`research/d31/staging/b7q118-round1.log`和`b7q118-round2.log`。
- `b7-management118`经生产Web完成声音和应用管理读取、同号去重及故障索引回归。没有写业务设置。

上述捕获目录均位于`research/d31/captures/2026-09-12/`。私有原件及失败不覆盖、不公开。基础117没有安装开发板，Windows、正式固件和下载地址保持此前发布状态。

## 尚未通过与正在处理的边界

- 原厂通讯录`b7-contacts118`返回`SecurityException`；真实系统日志确认AMS不接受未注册ApplicationThread绑定。只读`b7-contacts-peek118`确认组件匹配，但当前没有可直接取用的已发布Messenger Binder。未取得联系人列表，不冒充空列表，也不据此接通Web全量来源。下一阶段需要使用实际注册APP身份的按需读取路径，详见通讯录专项。
- Web工作线已正式发布媒体集合和故障页。浏览器已显示D31及“故障记录”，读取前正确要求选择电脑保全目录；原生目录选择和刷新恢复仍待真实UI验收，宿主队列通过不能冒充浏览器目录授权通过。
- 首次媒体能力拒绝测试的GET列表已确认空集合，但第一个`ptt`创建请求返回HTTP500非JSON，未继续其余模式。已向Web工作线交接；不重发结果未知的会话创建来掩盖失败。该轮宿主未保存非JSON正文，仅有长度和摘要，证据缺口明确保留并修正后续保全。
- 持续FD宿主首轮因ADB命令长度限制未发到设备；改stdin后遇到旧adbd的PTY回显，原帧门明确拒绝。两次失败分别保存在`b7-fd-window118`和`b7-fd-window118-stdin`，都未证明持续扫描稳定；继续修正无PTY只读传输，不改设备或重启ADB服务器。

## 收尾修正与实际边界

### 持续扫描观察

第三次`exec-out sh`试图输入脚本时超时，记录于`b7-fd-window118-execout`，没有取得观察样本。最终复用已证实的短`adb shell`，分节函数使最长命令1992字节，并按实测规范化CRCRLF，原始字节不改。`b7-fd-window118-compact`完整取得5轮约60秒间隔的原件，总窗口242.5秒，没有重启进程或整机。

初版宿主拒绝BusyBox默认颜色的FD列表，因此旧summary明确保留解析失败。修正后的严格SGR解析对同一原件离线回放，派生目录为`research/d31/staging/fd-window-color-replay-20260912/final-01/`，含逐项来源哈希和结果。5轮FD数量依次`28,28,28,28,25`，残留`boot_id`句柄全部0；每轮名称与链接列表一致，同PID/starttime/启动周期/实例/版本摘要/实际映射门通过，4次扫描比较均推进，健康新鲜、上报已确认，派生解析异常0。该窗口未见累积泄漏，不扩大为长期、冷启动或全业务稳定。

最终宿主增加`--color=never`并保留严格异常拒绝；14组离线验证通过。新命令选项仅在本机兼容测试中核验，本批真实窗口使用旧有色输出加独立回放，两者不混记。

### 媒体与故障Web联调

Web工作线查明能力拒绝异常从并发门回调逸出导致平台对象重置，修复提交`445843e788be59e8ce89be86a5ef307106a15e97`已部署；新增只读查询确认当前无遗留会话后，本任务进行新验收。`b7-media-modes118-fixed`完整保存原始HTTP正文和元数据，六模式均返回精确JSON400能力拒绝，前后同一D31的活动摘要、版本、健康及列表空集合保持。旧500正文缺口不补造。

浏览器生产截图保存于`output/playwright/d31-b7-20260912/fault-page.png`，含私有界面信息，不公开。浏览器目录选择、真实目录内保全及刷新恢复仍待操作；已有宿主1事件保全不能代替这些UI步骤。没有更改共享Web源码，所需修正由Web工作线完成。

### 最终源码与制品区别

活动输入归属原件经独立复核：06/07两组各一条真实活动输入匹配APP的PID/session及Policy句柄；05组跨启动边界保留未知；退出后的输入消失。新增独立解析器经过32项测试和11组原件回放，固定不授予自身录音豁免，尚未接入运行守卫。

该解析器不在机内118，最终源码组合另以基础119/完整120、`1.24.2-candidate`完成离线构建：754项执行、748通过、6跳过、0失败/错误，Lint、源码副本、签名及双制品隔离通过，目录`research/d31/staging/parallel-batch7-build120-offline/`。120没有安装设备或对外发布；机内继续保持通过设备验收的118，避免为未调用的独立解析器重复装机。正式Windows、固件和下载地址未改。

下一阶段优先补实际APP身份的原厂通讯录只读桥及来源合同，再按原计划推进网络设置写入与失联恢复、持续单向媒体的占用生命周期。客户端UI和丢失模式继续延期；首次系统迁移、完整实刷、双机及朋友设备业务修复效果仍按既定条件待验收。
