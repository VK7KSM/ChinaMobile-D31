# D31第四批远程业务并行开发与验收

## 开工与基线

2026-09-11用户明确要求按主计划第12.9节指派agent并行开发，总负责人统一指挥、汇总、接线和验收。当前已验收基线为完整102、基础101及Windows候选1.6.7-rc7，前三批源码提交为`1dfc276`。本批不重新构建Windows或固件，首次迁移、实刷、双机比较和实际故障修复效果按用户要求延期。

## 已派发任务

| 工作线 | 负责人 | 独占源码范围 | 首批交付 |
| --- | --- | --- | --- |
| 系统/网络/通讯录 | Newton | `management/`及对应测试、工具、专项文档 | 对接现有任务的API23系统设置执行与回读、网络状态/回退合同、通讯录真实接口适配 |
| 轨迹数据 | Pasteur | `telemetry/`及对应测试、工具、专项文档 | 真实定位/精度/时间、电量及供电采集，报告字段适配和文件合同缺口核对 |
| 媒体/告警/丢失模式 | Dirac | `media/`、`lostmode/`及对应测试、工具、专项文档 | 按需媒体及告警模块、资源释放；丢失模式最新合同与非破坏性API23适配 |
| 故障取证 | Fermat | `faults/`及对应测试、工具、专项文档 | 有界真实故障源采集、事件去重、原件冻结、容量约束及查询索引 |
| 总集成 | D31_dev | 公共任务/报告/能力/权限入口、组合构建、设备与Web验收及提交 | 按真实现有协议接入模块，每项分别记录源码、离线、设备与Web状态 |

源码范围均位于`research/d31_adb_bootstrap/app/src/main/java/net/elfradio/d31bootstrap/`下。agent直接编辑独占文件，不能操作设备、修改共享Web/D22源码、改版本或运行共享Gradle。原件保留，测试输出独立归档。所有任务已发出，当前状态为开发中，不代表功能验收完成。

## 接线与交付要求

- 先查现有Web真实任务类型、字段及回执；模块给出调用API和能力条件，由总负责人接公共入口，不能各建一套命令协议。
- 云连接、自更新、ADB、文件和账号配置直接复用；同号任务不重放，维护冲突及不确定结果按既有合同处理。
- 网页轨迹读取历史不唤醒设备；缺失定位或供电数据不伪造。媒体与故障原件使用不同保留合同。
- 媒体、定位和大日志不进入同步开机路径。实际声音、摄录、网络修改及锁屏由总负责人按明确目标逐项安排；本批不执行清除数据或自动清除到期测试。
- 每个独立模块可先提交可用小批，后续功能继续排期；不把占位类、纯模型或D22通过冒充D31业务实现。
- 需修改Web时按主计划第6.10节先说明具体范围，当批最后协调；现有接口联调继续进行。首次系统迁移与双机条件不阻挡本批单机业务开发。

## 首批源码交付与组合检查

2026-09-11四位agent已交付冻结源码，原分工与独占范围保持。系统管理独立26项、轨迹19项、媒体30项、故障36项测试通过，各专项文档保存成功及失败证据；独立结果不代替组合或真机验收。

- 公共入口已接`system_config`，原像持久保存后才写入；同号中断无结果不重放，回执须包含真实回读，失联网络写入明确拒绝。系统读取能力在独立后台只读核验后再声明，检查不阻塞报告。
- 报告读取真实定位缓存与电池广播，单次最多等待250毫秒，最多一个未完成采样，不主动定位或扫描无线网；待补传报告按原件发送，保留原十五分钟节拍。
- 自动故障归档独立调度，每60秒检查一次，系统完成启动且无维护预留时才运行有界后台扫描。不等待云注册，云身份拒绝停止云请求但保留本地取证与维护；修正身份后由既有入口重启核心。当前原件保存在本机，经已有文件与根命令通道取回，尚无自动上传队列。
- 媒体与告警模块只完成源码及离线验证；Web的麦克风/视频使用WebRTC，不能把本地AAC文件当成已联通网页媒体。SIP实时占用来源尚未验证，不启用告警能力；丢失模式仅只读接口，写能力全部关闭。
- 首次组合目录`research/d31/captures/2026-09-11/parallel-batch4-check-212828-165/`保留`mergeInto`接口名称不一致的编译失败，已统一为`mergeReport`。
- `research/d31/staging/parallel-batch4-candidate-20260911-104/`保留490项通过、5项跳过和Lint三项失败；缺失时区权限、可选相机声明以及API21权限检查已分别修复。`parallel-batch4-candidate-final-20260911-104/`组合测试、Lint及签名检查通过；随后分离管理能力核验与遥测线程，最终候选需重新构建核对，不复用旧APK。

## 单机验收预登记

目标为已有系统完整版102的授权D31开发板，机型须实读为`hct6735_66_m0`及`hct6737t_66_m0`，API23。完整设备序列号及构建指纹仅保存在私有捕获中；所有ADB命令固定电脑5042，不触及其它设备或ADB服务器。

本批候选为基础103、完整104，版本`1.21.0-candidate`。只将完整候选通过已验收的同包安装/后台自动接替路径交付开发板；基础不安装，系统监督102与系统APK原件保留。当前宿主未注入云更新签名环境，本批使用既有ADB同包更新工具，不重做Windows资源或改服务器发布记录。

安装命令使用`tools/Install-RemoteFullCandidate.ps1`，显式传入设备、候选路径与最终SHA-256、版本104、原活动102摘要和全新捕获目录。宿主先拉取已安装APK和系统APK、保存活动指针/健康/日志，预检通过才执行`adb -P 5042 -s <D31完整序列号> install -r <完整候选>`。预期安装与实际核心均变为104，系统特权身份及原件保持、MQTT和本地维护新鲜；否则保留现场，使用已备份原APK与既有手动接替恢复路径，不刷分区或清数据。

验收按顺序进行：实际APK/核心映射与权限、五组只读系统配置、真实遥测缺失语义、自动故障档案及Web查询。同包升级会增加故障原件和任务记录，但不改业务设置、不切网络、不重启整机、不拍照录音或播放告警。只有真实证据确认后才追加通过结论。

## 104单机结果及接口修正

- 交叉审查发现并修复：慢于250毫秒的采样可能一直赶不上十五分钟报告；新鲜结果被无条件替代采集丢弃；系统设置任务下发后取消未传到执行层；独立读取结果未核对完整规范请求。现有实现采用最长五秒异步准备、最多250毫秒单次等待、任务取消文件和完整请求绑定。取消发生在已完成任务后不会篡改原结果；强杀/断电后的业务原值自动恢复仍未验收。
- `parallel-batch4-candidate-reviewed-20260911-104/`最终500项测试通过、5项跳过、零失败，两种Lint及制品签名/身份隔离通过。完整104为491167字节，SHA-256=`8302f4f41d0cfd0e8bcb1cd7cb48186c5126068fddd894e1d8ab727900c0fa2f`。
- 104通过`Install-RemoteFullCandidate.ps1`同包接替；原安装及系统APK已备份，实际ART映射、系统特权身份、本地8765健康、MQTT和生产报告确认。没有重启整机、替换系统监督或修改启动入口。证据位于`research/d31/captures/2026-09-11/parallel-batch4-install104/`、`parallel-batch4-runtime104/`及`parallel-batch4-cloud104/`。
- 生产Web的声音、时间读取成功；网络读取失败并如实返回`SecurityException`，原失败任务保存于`parallel-batch4-management-web104/`，没有改网络或以空结果掩盖。只读诊断明确异常来自原厂`WifiManager`构造时注册应用接收器：root独立进程的`ApplicationThreadProxy`不在AMS应用进程表，增加Manifest权限不能修复此身份问题。
- 电池上下文查询也出现同类接收器身份异常，原输出保存为`telemetry-check-private.json`与`battery-interface.txt`。定位缓存返回`permission_denied`时保持缺失，不用IP位置代替。后续改用本机实读的`IWifiManager`只读方法及无应用线程、无实体接收器的粘性电池广播查询。
- 104自动故障扫描已真实运行，发现ANR、tombstone及DropBox原件；生产Web已通过既有`root_exec`查询索引，证据为`parallel-batch4-faults-web104/`。状态`PARTIAL`保留目录限额与故障前连续日志缺失，`capture=COMPLETE`仅指该次选中源文件字节，不能宣称完整故障链或根因已确认。历史原件可能早于监视器启动，重复观察次数不是崩溃次数。当前自动上传未接通，容量达到上限会停止新增并报告限制，不自动删除取证原件。

修正候选改为基础105/完整106、版本`1.21.1-candidate`，只修正上述实测系统接口。继续使用同一同包更新工具，原像改为104摘要，安装前先备份104；其余单机限制保持，106结果另行追加。

## 106组合及生产Web验收

2026-09-11最终源码位于`research/d31_adb_bootstrap/`，组合制品与完整测试位于`research/d31/staging/parallel-batch4-binderfix-20260911-106/`。517项测试通过、5项跳过、零失败，两种Lint通过；生成输入182项与源码逐项一致，基础/完整清单、依赖、包名及原签名检查通过。

完整106为492393字节，SHA-256=`19ba959259b7a36567ea07e7953d3c8da852fa98feace39f8e7295fce48debdb`。已同包安装并由原监督自动切换后台核心，实际106进程映射、新鲜本地健康、MQTT及服务端确认通过；未重启整机、未改系统监督和启动入口。安装及只读证据分别为`parallel-batch4-install106/`和`parallel-batch4-runtime106/`，均在当日私有捕获目录下。

| 检查项 | 实际结果 | 证据目录及边界 |
| --- | --- | --- |
| Web系统读取 | 声音、时间、网络、Wi-Fi、应用五组通过；同号重复查询返回原任务 | `parallel-batch4-management-web106/`；没有执行设置写入、切网或停用应用 |
| 网络实际接口 | 原厂`IWifiManager`五个只读方法适配通过，绕过不适用于独立进程的接收器初始化 | 104异常原件仍保留；网络写入与失联恢复未完成 |
| 电池及供电 | 六参数粘性广播只读返回成功；本次原始广播为`level=0/scale=100/plugged=2/present=true`，来源为USB供电 | `parallel-batch4-runtime106/telemetry-private.json`；这是固件广播原值，不据此宣称D31有真实电池或电池故障 |
| 轨迹历史 | 按实际报告编号找到服务端历史，电池/供电/存在状态及位置缺失原因与实读一致 | `parallel-batch4-history106/`；没有唤醒定位，GPS权限不足；服务端IP区域没有冒充精确GPS |
| 自动故障及Web查询 | 独立扫描、原件与前后采样索引持续保存，Web根命令查询通过 | `parallel-batch4-management-web106/`；不把历史故障视为本次新崩溃，不宣称自动上传或完整根因 |

基础105仍约133KiB，完整106约481KiB，基础不含本批云业务模块。Windows候选仍内置已验收基础101，正式固件仍为原批准版本；本批没有重打EXE/固件或修改正式下载地址。安装后冷启动、真实业务设置修改/恢复、通话音频及长期资源回归未在本批执行，不能用同包更新正常代替这些验收。

## 下一批分工

- 系统线：网络修改的独立失联回退、原厂通讯录Provider及列表/修改合同；先做真实只读结构核对，再接写入。已通过的账号配置不重做。
- 数据线：解决D31独立进程的定位调用身份，保留真实来源/时间；补最新版网页多选、目录取回和最终文件落盘验收。
- 媒体线：复用现有WebRTC会话实现D31媒体通道，确认可靠SIP/其它音频占用来源，再启用告警；按D22_Web_Dev最新已部署丢失模式合同继续API23适配，仍不做真实擦除。
- 诊断线：故障原件补传、容量到限后的可管理归档和多批聚合、运行载荷与配置语义基准；三方双机及真实修复效果按用户要求延期。
- 总负责人：继续掌管公共接口、能力声明、签名、设备及Web验收和统一提交。当前源码完成不等于全部远程业务已完成。

### 故障原件取回补充

`parallel-batch4-fault-return106-fixed/`已通过现有`get_file`取回一份冻结事件索引与对应尝试报告，电脑端保存后的字节数与索引SHA-256逐项一致；没有只凭设备上传成功宣布下载通过。此处验收的是诊断JSON原件，尚未取回该事件完整原始ANR/tombstone日志，不把两者混称。首次宿主误将任务查询响应当作含`device_id`，在下发前拒绝；已改为核对保存的原请求与任务号，原目录`parallel-batch4-fault-return106/`保留，不归因为设备文件故障。
