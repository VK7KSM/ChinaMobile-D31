# D31已安装APK来源核验

本批沿系统组件与权限核验继续追溯五处声明字段差异，客户端保持完整190。新增电脑端固定取号包采集工具，复用现有远程命令和文件回传，不增加设备启动任务，不安装或修改设备。

工具绑定目标及运行版本，查询实际安装路径，检查前后文件状态、摘要与启动身份，取回原件并本地回读后才确认清理。当前限单包、单APK、8MiB；中断沿原编号恢复，不重复下发。D31使用系统原生stat，每项shell断言显式失败退出。

## 验收结果

- 工具及既有报告采集回归共27项通过，包括真实shell断言、中断续作、文件变化与清理时序。
- 三台均保全1496030字节的相同APK，摘要为 `3b69433f5c56dd253f1e0ee5e323ecfd5859c7dbecd9b6f19332f78e15205fa8`，前后校验通过且暂存清理完成。
- 正式1.4.4镜像的取号占位包与运行时标签资源不同。正式交接通过绑定挂载覆盖安装路径；三台实际挂载资源相同，包管理器保留的已解析元数据不同。
- 四取号入口在三台仍有效禁用，发送操作被忽略；远程手动入口均有效启用。不因原始字段不同而重新安装或启用组件。
- 一台最终快照曾超过等待窗口，恢复查询原任务后完成；不据此宣称远程延迟问题已全部消除。

## 边界与后续

当前磁盘文件、系统种子、包管理器声明与有效状态必须分层比较，不能仅按版本号判断来源，也不能把挂载资源当作包管理器已重扫的证明。精确扫描时间未验证。本批是有限包来源核验，不是全系统一致性或修复业务验收。

后续继续补充有明确来源的配置预期并接入既有修复执行器。正式固件1.4.4、Windows1.6.7及客户端190不随电脑端工具重建。私有身份、原件与会话不公开。
