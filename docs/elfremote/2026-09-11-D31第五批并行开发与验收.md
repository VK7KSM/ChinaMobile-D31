# D31第五批并行开发与验收

## 基线与范围

2026-09-11按用户“继续开发工作不要停”的要求启动。本批承接第四批完整106、基础105与源码提交`55a1c0b`，不重做已通过的账号、自更新和文件底层通道。首次普通基础转系统、完整固件实刷、双机比较和真实故障修复效果仍按已有约定延期。

## 并行分工

| 工作线 | 负责人 | 本批具体交付 |
| --- | --- | --- |
| 系统管理 | Newton | 原厂与安卓通讯录接口核对、只读适配；已有有限设置写入的回读和恢复审查 |
| 遥测 | Pasteur | 定位权限不足的精确异常及调用身份核对，缓存读取修正及缺失原因保持 |
| 媒体 | Dirac | 核对可复用的真实WebRTC实现、最小接线方案和模块；可靠音频占用来源 |
| 故障取证 | Fermat | 完整事件原件导出、摘要清单、显式归档及容量状态 |
| 总集成 | D31_dev | 最新文件管理联调、公共接线、组合构建、设备和Web验收、统一提交 |

四位agent只修改各自模块、测试及专项文档。总负责人统一操作D31，使用本机5042服务器与完整网络序列号，不操作其它设备。共享Web仅核对，不自行改动；需要配合的字段和页面范围放在当批最后追加到`remote.md`。

## 验收边界

- 本批不写boot/recovery，不清数据、不开启自动清除，不主动切网或重启整机。
- 定位先读缓存，不主动定位或扫描；读取不到时保留真实原因。
- 故障原件保留，导出成功和电脑最终校验必须分别有证据；归档不能冒充已释放磁盘。
- 不把D22媒体或锁屏通过视为D31验收，未接通的能力保持关闭。
- 设备更新前备份当前APK与活动载荷，使用已验收同包覆盖和后台接替；每次证据使用新目录。

## 当前状态

已派发四条任务，正在开发。尚未生成新APK、安装或执行本批Web验收。

## 已完成的单项实读

- 生产Web返回完整106在线；有线ADB5042通路正常。没有操作其它设备。
- `parallel-batch5-file-tree106`通过现有Web任务创建独立合成目录、读取4页共14文件及2目录，确认权限、时间、中文名称与空文件；中文非空文件及空文件经Web实际下载到电脑并逐字节核验。浏览器另行确认D31文件页面目录读取、两目录同时选择、第二页两个剩余条目。未测试浏览器本地目录选择器及批量下载最终落盘，不把接口验收等同完整UI验收。
- `parallel-batch5-location-read106`确认：当前root进程定位权限查询通过，GPS提供程序存在且已启用，但框架和直接Binder的缓存查询均返回`SecurityException: invalid UID 0`。实际UID没有绑定包，系统Context的`android`包属于另一个UID。此结果定位到调用身份合同，未改权限或定位开关。
- `parallel-batch5-contacts-read106`确认安卓与Nexui两个Provider存在、启用且外部只读引用可用，标准号码表结构查询成功。没有输出联系人值，没有查询未知原厂列表路径。

以上目录位于`research/d31/captures/2026-09-11/`，包含实际诊断JAR、命令、构建身份及原始输出。诊断JAR只放在全新设备临时路径，未安装APK或改变系统业务。

## 组合审查与修正

四线已交付：系统线47项测试通过，遥测线48项通过，媒体线50项通过，故障线58项通过、1项Windows符号链接权限条件跳过；故障ZIP宿主另有18项测试通过。这些专项结果不替代组合构建或设备验收。

- 故障归档的周期全包哈希已改为真实stat身份缓存，显式导出和归档仍全量核验。归档只释放活跃事件名额，磁盘原件保留，不显示虚假的已释放容量。
- 位置缓存读取改为按需、不导出的应用Service，以真实应用UID访问框架；独立核心等待上限4秒，报告准备窗口保持5秒。没有增加开机任务或主动定位。
- 媒体依赖仅进入完整版，ABI限定为D31的32位ARM；基础APK严禁包含WebRTC类和原生库。实际音频采集及网页会话未验收，能力仍关闭。
- 主任务只读宿主已增加每次唯一设备退出标记、CR规范化、30秒ADB客户端等待上限与原始输出归档，超时只结束该宿主客户端，不停止ADB服务器。
- 组合构建原始失败分别保存于`parallel-batch5-build108`（SDK环境未传入）、`parallel-batch5-build108-sdk`（实际原生库测试路径未接线）及`parallel-batch5-build108-native`（诊断类隐藏API的直接引用被Lint拒绝）。没有复制旧APK冒充本批通过制品。前两项已修正公共构建脚本，最后一项由遥测线修正API23精确反射调用。

本批设备候选计划为基础107、完整108，版本`1.22.0-candidate`。目标仍为已授权D31/API23；安装使用`Install-RemoteFullCandidate.ps1`同包覆盖，先保存106安装APK、系统APK和活动指针。预期只有应用与活动核心切换，系统原件及业务数据保留；如安装或接替失败，保留现场与旧APK，按原同签名恢复流程处理，不写boot/recovery或擦除数据。最终是否通过以随后追加的真实记录为准。

## 最终构建与设备接替

2026-09-11最终构建为`research/d31/staging/parallel-batch5-build108-final/`。598项组合测试中592项通过、6项条件跳过、零失败；两种Lint、207项生成源码/测试副本摘要、原签名及两种制品依赖隔离检查通过。

| 制品 | 版本 | 字节数 | SHA-256 |
| --- | --- | --- | --- |
| 基础探针 | 107 | 135644 | `1e67ecca450996ddb9fe8429cce775980aa226b31ef4b466b9efffa77e0028e2` |
| 完整elfRemote | 108 | 4586740 | `7ab7a082f105996fc5027c7eba50e945dc55290422353568a36f20ecf25d74a0` |

完整108已同包覆盖安装，活动指针、安装版本、真实ART映射和Web版本均一致。系统原APK摘要保持，原系统监督与8765急救服务未替换，没有重启整机。`parallel-batch5-install108`保留106安装原像、系统原件、交接回执及实际身份；`parallel-batch5-runtime108`保留进程映射、健康状态、供电与报告回执及有限日志。

## 真实功能结果

- **定位缓存：**`parallel-batch5-telemetry-read108`实测46毫秒，返回`no_cached_location`，不再报`invalid UID 0`。真实应用Service创建、处理请求并销毁的日志齐全，随后服务列表为空。证明此次跨进程缓存请求成功且没有缓存；不证明设备已取得GPS位置或超时故障路径真机通过。
- **轨迹历史：**`parallel-batch5-history108`按实际报告编号核对既有Web历史，真实电量、供电、充电与缺失位置原因一致；没有把缺失GPS改写为实测定位。
- **系统管理：**`parallel-batch5-management-web108`生产Web声音、时间、网络、Wi-Fi、应用五组读取，以及同号任务去重、故障索引均通过。读取成功不代表尚未实施的网络切换、设置写入及恢复通过。
- **SIP与维护：**`parallel-batch5-sip108`确认已配置的Nexui和QUIK线路新鲜已注册；本机8765仍返回root急救健康状态。没有拨号、发短信、切网、操作其它设备或验证冷启动。
- **原生媒体库：**首次默认`app_process`检查返回加载失败，已保留`parallel-batch5-native-read108`。实际系统入口指向64位，APK则明确只有32位ARM库。改用`app_process32`的独立helper后，`parallel-batch5-native32-read108`返回`loaded=true/process_64bit=false`，系统安装信息为`primaryCpuAbi=armeabi-v7a`。这只证明实际32位JNI加载；没有创建录音器、WebRTC连接、扬声器输出或更改原64位核心。媒体与告警能力保持关闭。
- **完整故障原件取回：**`parallel-batch5-fault-export108-paged`经生产Web导出、上传、下载、电脑验包、归档、独立查询全程通过。真实TOMBSTONE包53090字节，10个冻结文件含1份完整非空源原件，包摘要为`9c4eaa86b51da546e44be8ae4af588095eb4be4330b33cc5fa5f8f97447988b9`。原件逐项摘要和长度一致；归档不删除设备原件，`releasedBytes=0`。唯一清单缺口为`PRE_FAULT_CONTINUOUS_LOGS_UNAVAILABLE`，不能宣称完整事件窗口或根因已经确认。
- **Web回执大小：**首次`index 16`触发现有16000字符上限，宿主按`truncated=true`拒绝；失败原件保留在`parallel-batch5-fault-export108`。更正后每页1项，保留游标与最多128项边界；不是放宽截断判断或重复执行失败导出。

## 新故障与容量边界

验收时真实日志出现QUIK的`ConstraintTrackingWorker cannot be cast to androidx.work.Worker`异常。常驻采集器随后保存对应DROPBOX源；本地取回912字节压缩原件并核对摘要后，解压内容确实匹配QUIK包名和该异常。证据为`parallel-batch5-runtime108/new-crash-original-private.bin`及配套结果；其SHA-256为`bdc318d4eb9fcb9046387cb9477f22696c41d5a0d734e7aeeefd15fc1e08f32f`。本批未修改QUIK，不能凭时间相邻断言elfRemote导致或已修复该故障。

归档释放一个活跃槽位后，正常扫描接收新故障。随后状态为32个活跃、1个已归档、33个保留事件，保留3899671字节，上限67108864字节，索引错误0；仍因活跃数量达到上限而返回`CAPACITY_LIMIT`。本批实现的是显式导出/确认归档及容量可见，尚未实现持续自动上传和归档；不能声称已无限覆盖后续崩溃。下一批应优先完成可恢复上传队列、取回确认及源优先级，继续保留原件，避免历史源占满活跃配额。

所有原始捕获、故障内容、会话及设备身份只保存在本地；公开源码只提交实现、测试、工具和中文记录。首次基础转系统、实刷、双机一致性、真实业务修复、完整媒体及丢失模式均维持此前未验收边界。Windows、正式固件和下载链接本批不重打或更新。
