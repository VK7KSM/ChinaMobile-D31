# 2026-09-11 D31第二批并行开发验收

## 起点和工作范围

用户要求开始下一批并行任务。沿用首批提交`7bc35a5`及主计划第十二节，不重新开发已经验收的远程连接、云更新、离线核心交接、ADB或账号配置。机内实际状态由本批读取确认，不能把源码候选91/92视为已经安装。

本批按四条工作线同时实施，总负责人控制共享入口、构建、D31操作、联调、证据和提交。原始制品及失败记录保留，所有新输出使用独立目录。

## 分工及验收门

| 工作线 | 独占范围 | 本批交付及判定 |
| --- | --- | --- |
| 设备取证 | 客户端`diagnostics/collection`及对应测试 | 显式范围的文件清单和按需有界故障证据索引；Android真实文件属性和不跟随链接；预算耗尽、读取失败及不稳定不能记作缺失或完整 |
| 固件清单 | `tools/firmware`及专属记录 | 以真实安装映射和源摘要生成固件预期清单；缺项、重复目的、越界、错误摘要拒绝；初始化和未读取范围明确未检查 |
| 修复事务 | 客户端`repair`及对应测试 | 原像前置条件、备份、暂存、切换、实际回读、失败恢复和中断续查；先以隔离文件适配器验证，不直接开放任意系统写入 |
| 基础探针接替 | Windows新增准备工具及`BasicProbe`测试 | 校验实际基础APK并准备独立内置资源，分类普通安装、系统预置、更新副本及核心状态，形成明确接替前置条件 |
| 总负责人 | 共享维护入口、状态取证、部署验收及提交 | 将可用模块接入原命令和文件通道，核对真实D31副本和运行来源，集中完成组合构建、受控设备验收和来源清单 |

## 接口和设备边界

- 保留第1版诊断合同。单批上限4096项；超过上限必须拒绝或标记不完整，不能丢掉尾项后宣称全系统一致。多范围采集与真正聚合是不同验收项。
- 取证先按需执行，不新增常驻监听或开机扫描；故障日志和快照先存本地，后续沿用现有私有文件通道，不把大日志塞入普通状态报告。
- 修复方案使用明确受控文件和可验证原像，不从一个差异直接推导根因或自动改写系统；boot、Recovery及私人账号不进入本批文件事务。
- 只操作授权D31，电脑ADB服务器固定5042，使用读取确认的完整序列号。各代理不直接连接设备、不改共享Web、不独立提交或发布。
- 真实基础到系统完整版接替必须按设备现状分类。已有系统完整版不能通过卸载、清数据或强行降级伪装成全新机器。若本批只能验证其中一种状态，记录具体覆盖范围。
- 需要修改Web的部分先列协议和页面缺口，作为当批最后交给用户协调；未实现的新服务端能力不能仅凭客户端代码宣告可用。

## 当前进度

四条工作线已经派出。总负责人已确认D31专用ADB可读；接下来保留新鲜运行基线，开发独立状态采集和维护入口。此处仅记录启动事实，后续实现、测试失败、修正、真实验证和提交结果继续追加。

## 运行副本只读采集预登记

- 当前D31构建为`alps/full_hct6737t_66_m0/hct6737t_66_m0:6.0/MRA58K/1583081804:userdebug/test-keys`，已安装90版且为系统特权应用更新；包管理器另有75版隐藏登记。先不把隐藏登记解释为实际系统APK版本。
- 新增按需`RemoteRuntimeInventory`，分别读取实际安装原件、系统原件、活动指针并校验所指APK、安装身份及有限核心健康字段。旧健康文件、未来时间、读取失败和原件不符不能生成正常结论；自报健康匹配不等于已证明实际加载代码。
- 四项离线测试通过。编译和测试证据为`research/d31/staging/batch2-runtime-inventory/`。独立JAR的SHA256为`2294ed80795692996210da3beb5e381f10802b5353c9a82cb6e68ab36539b899`，配合上一批完整92候选（SHA256为`a3b9540588677ceed6c6393b20febb3b43ed40e65682ff575b94ba8a7f33b4c8`）作为类依赖。
- 本次只向新`/data/local/tmp/d31-batch2-runtime-20260911`目录放入JAR及依赖APK，并以`CLASSPATH=<JAR>:<APK> /system/bin/app_process /system/bin net.elfradio.d31bootstrap.RemoteRuntimeInventory`调用只读入口。精确命令、序列号及时间单独保存于私有捕获记录。
- 不安装APK，不改变系统原件、指针、核心、权限、启动入口或网络。预期输出各副本实测摘要和证据状态；出现异常只保存输出，测试文件保留作证据，无系统改动需要回滚。

## 运行采集首次异常及修正方向

- 首次独立进程未生成JSON，真实输出为`Segmentation fault (core dumped)`。崩溃日志确认仅该`app_process`收到SIGSEGV，原监督、90版核心及Nexui进程保持，核心新鲜健康回执继续正常。
- 宿主旧ADB返回值未传播该进程失败，第一次JSON解析才识别异常；后续判定必须同时要求设备侧明确退出标记及完整JSON，不能以宿主ADB退出码代替。
- 首次JAR包含重新编译的`RemoteUpdatePlatform`及其嵌套类，同时类路径又包含已有APK，存在重复业务类装载。它是待排除变量，尚不足以断言就是SIGSEGV根因。已将安装身份查询放回采集类内部，恢复原更新适配器，后续JAR只包含新采集类，不覆盖基础APK里的类。旧JAR、输出及崩溃日志保持原样。
- 修正后四项离线测试通过，实际DEX确认仅有9个新采集类定义，没有覆盖原APK业务类；新JAR摘要为`6fb9e6e6665bf87373e28f308bc153b5cc3330d4ddf5ebbd267522ac8e2803f1`。依赖APK不变，下一次在新的独立目录执行同一只读采集，同时读取设备侧退出标记；不修改活动服务或系统文件。
- 新独立进程返回明确`D31_EXIT:0`和完整JSON。实际安装包90、系统APK原件85、活动指针所指APK90；实际安装身份为系统特权应用更新，原件摘要与活动声明一致，核心健康文件新鲜且自报90版匹配。包管理器75版隐藏登记不能替代这些实际原件证据。
- 此结果证明本次读取范围内的副本和状态，不把系统85与更新90这一分层状态当作待自动修复错误；实际加载代码仍标记未检查。去除重复装载后本次成功，但没有将前次SIGSEGV单因归结为已经完全定位的ART缺陷。

## 组合构建及诊断真机预登记

- 四条代理工作线已冻结。统一组合构建完成，326项Android测试中321通过、5跳过，0失败；两种制品的Lint、实际源码模型、身份、原证书与依赖隔离核验通过。目录为`research/d31/staging/parallel-batch2-candidates-20260911/`。
- 基础93版为135646字节，SHA256为`3ab6fe5dfcfd93f0438cc6585e4342b0d0e5cc181c19ed423a51a3e1c4aec294`；完整94版为426412字节，SHA256为`9099f3c8c8256befccfb7a150f19e1c9d82c06b866bf4981da7cf4cf8595300c`。均为`1.19.1-candidate`序列，未安装或发布。
- 新增`RemoteDiagnosticCommand`与持久任务存档，沿用现有8765的`POST /exec`及结果查询。操作为运行副本、明确范围清单、明确故障文件；原报告保存在私有任务目录，短回执提供路径、长度、摘要。相同请求复用原件，参数冲突拒绝，中断任务不重扫。没有常驻线程、开机扫描或新增云协议能力声明。
- 目标仍为上文同构建D31，现有核心90继续运行。本次通过`Test-DiagnosticDevice.ps1`，电脑5042的定向ADB只传输完整94候选及取回报告；8765执行独立`app_process`。仅在全新`/data/local/tmp/diagnostic-*`、`/data/local/d31-diagnostic-input/diagnostic-*`及诊断报告目录创建候选、合成文件、软链接与报告，不安装APK、不改活动指针、系统文件、服务或启动状态。
- 预期隔离目录枚举3项、链接只记录目标、合成故障原件16字节可取回且摘要一致，同号重试报告不变；随后只读采集`/data/local/d31-recovery-entry`，与真实1.4.3固件映射比较。限定范围完成不代表整系统一致。精确命令、时间和设备标识由宿主脚本保存在本地私有捕获目录，不复制到公开仓库。
- 任何检查失败即保留候选与原始日志并停止后续步骤；现有核心和系统没有被替换，无需刷写回滚。测试目录保留，不清理失败原件。该验证不能代替APK手动升级、活动核心交接或朋友设备验收。

## 现有系统完整版接替预登记

- 按既有同包更新授权继续验证完整94候选接替现有90版；基本版不安装到已托管设备，不通过卸载或清数据模拟首次安装。候选哈希与上述统一构建完全相同，目标构建保持不变。
- 先保存现有安装90原件、实际系统85原件、活动指针、健康记录、包管理状态、进程、挂载及有限日志。安装原件哈希须等于`19d353578ec9d06a49a37b731f82aae07b1df49d17d5d215e24874f50929a336`，否则停止变更。
- 唯一安装命令为D31专用5042服务器和完整序列号下的`adb install -r <上述完整94候选>`。复用已验收的手动APK更新监督流程，最多等待180秒，要求本地事务`success`、安装/活动94同摘要、特权身份保留，并进一步核对独立进程映射和真实Web报告。系统85、早期8765、系统支持与启动脚本不修改，不重启整机。
- 失败保留现场和原回执，不重复安装。已有监督器按记录保留的90原件回退；如其未完成，先检查实际安装和活动状态，再使用备份90原件按既有受控恢复路径恢复并核验，禁止卸载或清数据。宿主脚本及原始证据存于`research/d31/captures/2026-09-11/parallel-batch2-full-handover/`。

## 本批真实验收结果

1. 清单与故障取证：统一94候选在独立进程执行成功。隔离目录3项完整，耗时73毫秒；普通文件内容摘要、0600权限、root属主正确，软链接只记录`sample.txt`目标且不作普通文件哈希。同号重试报告逐字节一致。16字节合成故障原件经ADB取回，摘要与索引一致。原始捕获位于`parallel-batch2-diagnostic-device/`。宿主随后补入上述字段自动断言并离线复核同份原件，没有重新扫描来替代原结果；不同范围后续使用不同快照编号。本轮测试身份不是正式开发板基准。
2. 实际范围采集：`/data/local/d31-recovery-entry`枚举110项，耗时3447毫秒，限定范围状态`COMPLETE`；SELinux、扩展属性、实际生效源等仍为未检查。没有采集朋友设备，不存在双机或全系统通过结论。
3. 固件比较：真实1.4.3 ZIP及展开目录均得到54条预期项，45项有来源、9项未检查；始终为`PARTIAL`。与本次D31交集5条路径的30字段比较得到19相同、1不同、10缺证据。差异是`enabled`内容摘要不同；其类型、权限、属主相同。固件安装后首次启动前与现场运行后的阶段不同，不能仅凭该差异判定损坏或改写标记。详情见固件专项记录及其二方派生证据。
4. 真实云通道：现有生产`root_exec`成功执行已完成诊断请求，返回原报告回执；第二个云任务回读摘要一致。升级前和升级后分别验证，使用原件缓存，没有重复采集。证据为`parallel-batch2-cloud-diagnostic/`及`parallel-batch2-cloud-managed94/`。没有新增服务端协议、专用页面或诊断能力标志。
5. 真正接替：90版同包覆盖安装94成功，原监督流程在第四次轮询前完成本地事务`success`。安装APK及活动原件均为94且摘要匹配，系统原件仍85且SHA256未变化，系统特权身份保留。独立`/proc`映射取得3条与94载荷对应的实际映射；限定时间窗口的原件与映射证据单独保存，运行采集协议仍保守保持`loadedCode=NOT_CHECKED`，未因单次验收修改通用判断规则。
6. 升级后回归：生产API返回`1.19.1-candidate`及就绪状态，文件管理与ADB能力保持；Nexui已配置线路及QUIK均返回新鲜已注册，其余三个未配置槽位仍为未注册。安装后有限500行日志中未见`FATAL EXCEPTION`、`ANR in`或`Fatal signal`匹配；这不是长期稳定或离网冷启动验收。未重启整机，未改网络、SIM、账号、系统支持、早期8765或启动脚本。
7. 基础资源：93/94实物在Windows PowerShell 5.1和PowerShell 7.6.5各74项通过。93基础资源已准备，保持135646字节。正式Windows构建仍未接入新资源，旧EXE内置APK没有因此改变；不能把独立准备/导出/预检工具写成正式工具已发布。
8. 修复事务：22项离线实际临时文件测试通过，覆盖同号去重、备份、暂存、切换回读、中断续查、逆序恢复、外部内容保护和磁盘失败。测试适配器未编入运行APK，没有可用于真实系统写入的默认执行器，也未进行朋友设备修复。

## Web配合与下一批入口

- 本批通过现有`root_exec`和既有文件通道即可开发与联调，未修改Web代码。专用诊断操作、选择开发板/固件/目标机、报告分批管理、故障事件关联、修复方案/进度/回滚页面仍需Web配合；具体需求另由`remote.md`最新`D31_dev`条目登记，作为当批最后交付。
- 下一批可以继续并行：正式Windows基础资源接线与自检；基础至系统完整预置/更新副本迁移合同；更多实际属性及分批聚合；受控系统修复执行器与业务回读；系统设置、网络、通讯录等已有计划的独立模块。共享能力声明、权限及真机操作仍由总负责人统一。
- 本批已经验证“系统完整旧版到完整新版”，尚未验证“普通基础探针到系统完整预置”，不能通过已托管开发板强装基础或卸载来替代该场景。完整系统基准、朋友设备、完整固件重建与实刷、离网首次启动及最终UI仍未完成。
- 正式APK下载、EXE、固件、主分支及下载链接没有发布变化；仅本机D31已安装94开发候选，源码交付沿用开发分支。原始设备报告、会话凭据、失败目录和测试APK不纳入公开源码。
