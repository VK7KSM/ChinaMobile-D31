# D31第六批并行开发与验收

## 基线与目标

2026-09-11按用户“继续开发工作不要停”启动，承接完整108、基础107及开发分支提交`58230b5`。本批重点是故障新源发现和容量周转，以及真实应用进程内媒体接线。最新共享记录为D22的169丢失模式显示页与168有限验收，不能当作D31功能已通过。

## 并行分工

| 工作线 | 负责人 | 本批交付 |
| --- | --- | --- |
| 故障采集 | Fermat | 修正目录枚举截断与来源公平性、新源优先级；保持原件与容量上限，提供有界待处理索引 |
| 补传宿主 | Pasteur | 可恢复的导出、Web取回、电脑验包、显式确认归档队列；阶段持久化、稳定任务编号、预算与失败恢复 |
| 媒体应用服务 | Dirac | 非导出32位应用服务、独立核心控制与状态桥、准备/开始/停止/查询，复用已通过JNI的麦克风适配 |
| 音频占用 | Newton | 蜂窝、SIP与系统音频状态的真实只读证据、快照与AudioGuard；不确定时拒绝占用，不用高频dumpsys |
| 总集成 | D31_dev | Web媒体合同、公共入口与Manifest、构建与签名、D31及Web验收、记录和提交 |

各agent独占模块，公共文件、版本、设备操作和提交由总负责人处理；不修改D22/H13/Pixel或共享Web。需Web配合的精确范围在本批最后写入`remote.md`。

## 验收边界

- 不刷boot/recovery、不清数据、不切网、不重启整机；仍使用D31专用5042服务器及完整序列号。
- 原始故障文件保留，上传、下载、电脑验包和确认归档分别有证据。释放活跃槽位不是释放磁盘，部分源和事件窗口缺失不得被包完整掩盖。
- 媒体先验证真实应用身份、权限、JNI、可查询生命周期及可靠音频占用；未有完整证据不得开启整套媒体能力。原64位管理核心保持，实际音频载荷使用已确认32位应用进程。
- 不为本批重打Windows或正式固件。首次迁移、实刷、双机比较和真实业务修复仍依此前条件延期。
- 每项设备变化前记录目标、假设、命令、预期及原像/回退；捕获使用全新目录，中文记录追加保留历史。

## 开工状态

四位agent已获具体任务，尚未构建或安装第六批候选。当前D31仍为已验收108；本条不是完成声明。

## 23时只读音频基线

- 目标为现有完整108、API23的D31。假设为原厂会话Provider能够提供区别于SIP注册状态的通话占用信息；执行独立只读`AndroidAudioOccupancyCheck snapshot`，没有拨号、发送短信、录音或改网络。
- 命令宿主为`tools/Invoke-D31ReadCheck.ps1`，采用D31专用5042服务器及完整序列号，实际参数、构建指纹和时间保存在私有捕获`research/d31/captures/2026-09-11/parallel-batch6-audio108/`。诊断JAR摘要为`7f41c29820320df4e8c2eaaeeea541ac7c104359ff49616069092bec0e76f71f`。
- 观察：采样39毫秒；蜂窝`call_state=0/phone_count=1`，音频`mode=0/focus_gain=0`，10个输出流和9个录音源均无活动；Nexui原厂Provider返回`statuses=["IDLE"]`。退出0表示只读取证完成，不代表连续媒体会话已验收。
- 推断边界：本次能够读取实际会话状态，不再用“账号已注册”代替空闲判断。输入源布尔接口无法区分本客户端与其它应用的录音，正式持续采集仍需要进一步验证归属；当前入口仅准备和查询。
- 回退范围：只添加唯一临时诊断JAR，无系统配置原像被改写，无需恢复通话、网络或启动状态。保留诊断原件用于复核。

## 组合构建与上机预登记

- 冻结基础109、完整110，名称`1.23.0-candidate`。组合构建路径为`research/d31/staging/parallel-batch6-build110`；构建、Lint、原签名和双制品隔离均由既有`Build-RemoteVariants.ps1`检查，未通过则不安装。
- 目标设备仍为既有D31 API23。当前原像为完整108，摘要`7ab7a082f105996fc5027c7eba50e945dc55290422353568a36f20ecf25d74a0`。假设为同包安装后独立核心自动接替，新媒体服务仅按需创建，不增加同步开机负担；本次不验证冷启动。
- 安装命令采用`Install-RemoteFullCandidate.ps1`，显式传目标序列号、新APK路径/摘要/版本、上述原像摘要和全新捕获目录。先保存安装APK与系统APK原件，再执行`install -r`。预期活动记录、核心健康和实际映射切至110，系统原件摘要保持。
- 如接替失败，保存失败任务和日志；按原有手动更新回退合同恢复捕获的108原件及活动核心，不卸载、不清数据、不刷分区。候选未通过前不发布正式渠道或修改下载链接。
- 本次实际验收为Web查询、准备、查询；准备工作线程最多等首轮音频采样1500毫秒，权限、来源错误和未知均保留。应用入口只登记工厂，不加载JNI或等待。服务结束须关闭采样线程。
- 故障验收依次为只读`discover/pending`、宿主请求预算停止、同目录恢复并最多完成两条故障的取回/验包/归档。归档只释放活跃槽位，原件不删除；不安装定时补传任务，不制造崩溃。

## 构建结果

- 651项组合测试执行，645项通过、6项按既有环境条件跳过，0失败、0错误；两种Lint通过，225份生成源码/测试与原件逐项摘要一致。
- 基础109为135652字节，摘要`816bbe1a5c7195ccfa02d50d2b8f4c2f97fe4568c089307cbd3c63d397318b87`；完整110为4610067字节，摘要`03b3acd949c7bedfa5382f947bce29bbee7fc60e9683934200a3d678e6b0c5f6`。两者同原包名和原证书，基础未混入媒体依赖。
- 故障宿主独立40项测试通过。完整测试、签名和制品索引保存在上述构建目录及各专项索引；不把这些离线结果当作设备功能已通过。

## 110首次设备结果与必要修正

- 完整110同包安装、独立核心接替、系统身份和系统APK原件保持均通过，捕获为`parallel-batch6-install110`；Web上报版本名称`1.23.0-candidate`。首次宿主把版本代码110传作Web版本名称，已在下发任务前拒绝；改正参数后的新捕获独立保存，没有执行错误目标任务。
- Web媒体`query`成功，`prepare`在1314毫秒返回`MEDIA_LINK_PATH_REJECTED`。设备只读确认Android的`/data/user/0`是指向`/data/data/`的正常系统别名，缓存子目录尚未创建。准备路径检查误拒绝此别名，不是APK签名、网络或录音故障；服务随后已退出。修正仅限框架提供的应用私有根目录归一化，并继续拒绝子目录越界，不修改系统链接。
- 故障`discover`回执四类目录均为`UNSTABLE_DIRECTORY`，没有伪报全量成功。静态回查确认`AndroidCollectionAccess`将`mode`保存为`st_mode & 07777`，新枚举器却拿它与含文件类型位的原始`st_mode`直接比较。下一候选统一这两者表示，保留目录类型、设备号、inode、时间、权限及所有者核对。
- 故障`pending 16`元数据查询已返回真实容量状态，仍为32活跃、1归档、33保留。此项只证实读取，不代表新发现已通过。原110失败回执保留于`parallel-batch6-media-web110-vname`、`parallel-batch6-media110-failure`和`parallel-batch6-discovery-web110`。
- 这两项局部修正合入基础111/完整112后重新构建与验证受影响功能，110仍不标作本批最终验收。未改变启动脚本、桌面、通话配置或原始故障文件。

## 真实故障补传与恢复

- 通过生产Web使用已装110的原故障导出合同，宿主首次限制4次请求后以`ROUND_REQUEST_BUDGET`结束。第二次使用同一状态目录恢复，完成2条事件；86次有界请求、0重试，共下载281690字节，状态为`ROUND_EVENT_BUDGET`，无待处理项和阻断。
- 两份包分别7514和274176字节，都经既有独立验包器返回`HOST_PACKAGE_VERIFIED`。每条事件的导出、文件取回、归档和最终查询任务均只提交1次；原任务编号和分阶段快照可逐项追踪。
- 第一份有故障前连续日志和前置窗口缺口；第二份还存在采集截断，`sourceLogComplete=false`。包保存完整不等于完整故障窗口，缺口不会被归档成功覆盖，也没有声称查明根因。
- 两次设备归档都明确`originalsDeleted=false/releasedBytes=0/activeSlotReleased=true`。保留原件，只释放两个活跃槽位；不是清理磁盘或无限容量保证。
- 私有原件位于`research/d31/captures/2026-09-11/parallel-batch6-fault-queue110/`，逐项核对为同级`parallel-batch6-fault-queue110-verification.json`。这次真实验证的是请求预算结束后的持久恢复；强制宿主终止的锁释放和各阶段中断由独立离线测试覆盖，没有将两种证据混称为真机断网测试。
- 后续112不重复取回这些已确认事件，只验证修正后的发现/分页及保留容量状态。故障导出、归档合同和宿主运行载荷在本次两轮期间保持。

## 112修正验收与枚举性能缺口

- 基础111/完整112、名称`1.23.1-candidate`构建通过：657项执行，651通过、6跳过、0失败/错误；两种Lint与原签名通过。完整112为4610427字节，摘要`f3b115b80ace47ca3a1da781aa6f19289abd6a2576fa8172f31348b50f4dc261`。同包安装和核心接替通过，系统原件保持。
- 生产Web的`query → prepare → query`全部通过。真实APP32身份、活动APK摘要、JNI、录音权限、AppOps及三个占用来源均通过；APP首轮采样20毫秒，返回`PREPARED`。没有创建AudioRecord或媒体连接。证据为`parallel-batch6-media-web112`。
- 服务收尾时`dumpsys activity services`为空，APP线程中没有新增音频采样或媒体控制线程；APP已映射JNI，root核心已映射112对应的arm64 ART文件。近1200条日志未匹配本客户端致命异常或Nexui/本客户端ANR。此结论仅限捕获窗口，不代表冷启动、长时间运行或实际通话切换已验收。
- 原运行核对统计只找直接APK路径，误将ART编码映射标成false；已依既有`mapsArchive`合同核对3条对应映射，追加`parallel-batch6-runtime112/result-corrected.json`。原始统计保留，不把宿主统计错误当设备未接替。
- 修正后的`discover`在ANR目录完整枚举32项、接受10项，TOMBSTONE完整枚举10项、接受10项；`pending 16`为4059字节。归档后自动接纳2项，实读为32活跃、3归档、35保留、4195292字节；再次达到活跃配额时如实显示限制。
- DROPBOX实际目录有828个文件，但本次在同一1250毫秒来源份额内只枚举474项，其中333项匹配，接受64项，回执明确`SCAN_TIME_LIMIT/PARTIAL`。游标只轮转已看到的候选，不能解决稳定目录顺序下尾部持续不可见，因此此项不能收尾为完成。
- 已确认可消除的重复开销：匹配名称的每次完整路径`lstat`都重新核验所有祖先，而枚举器已经固定目录fd。下一候选只增加枚举期间有效的固定fd子项元数据读取，保留单层名称限制、叶链接不跟随、目录前后身份与选中候选的签名复核；不增加时间、字节或候选预算。实际同预算全目录覆盖通过后才确认性能修正。

## 2026-09-12零时：114最终单机验收

- 基础113/完整114、名称`1.23.2-candidate`组合构建完成：662项执行，656项通过、6项跳过，0失败、0错误；两种Lint、同包同签名及依赖隔离通过。基础为135655字节，摘要`a874f0808555e7d4340aa984ec251e6cf0f0519e9fce29055b88c90f1d47a9c4`；完整为4611517字节，摘要`46106ff0d892e155a2b9f5dbb85151226cd09d63f4e6d3098c9a276100f03993`。索引和测试原件位于`research/d31/staging/parallel-batch6-build114/`。
- 依前述预登记备份112安装APK和系统原件后，使用`Install-RemoteFullCandidate.ps1`执行同包更新114。实际参数、目标构建指纹、时间、原件、逐次健康和回执保存于`parallel-batch6-install114`。自动核心接替、系统特权身份及系统原件未变均通过；生产Web返回版本名称`1.23.2-candidate`和更新成功。未重启整机。
- 生产Web只读发现完整枚举ANR的32项、TOMBSTONE的10项及BOOT的35项，分别耗时25、17、25毫秒。DROPBOX已增长至834项，匹配579项，在476毫秒内全部枚举完成，选取64候选。原1250毫秒来源份额没有增加；耗时范围仅为枚举及子项元数据，不包含后续签名读取。
- DROPBOX的`PARTIAL/CANDIDATE_PAGE_OR_READ_LIMIT`仍准确保留：这是候选分页限制，`enumerationComplete=true`才证明目录枚举完成，不能将其解释为579份原件全部捕获。`pending 16`为4047字节，返回32活跃、3归档、35保留、4198595字节及`ACTIVE_EVENT_LIMIT`；该容量是`LAST_SCAN_NOT_LIVE`。本次只读发现未修改归档。
- 最终媒体`query → prepare → query`再次经生产Web完成：APP32、活动APK、JNI、权限及AppOps符合；真实首采18毫秒，快照年龄20毫秒，三个来源均空闲。没有创建AudioRecord或媒体连接。`managed_media=false`保持，不能把准备成功写成可持续录音。
- 退出检查确认媒体服务为空，无音频采样或媒体控制线程残留；APP映射JNI及自身APK，独立核心映射114对应arm64 ART文件。最后1200条日志没有匹配致命异常或指定Nexui/elfRemote无响应。核验仅限该窗口，不代表完整历史、冷启动或长期稳定。
- Nexui已配置线路及QUIK线路均有新鲜注册结果；未配置的其它三条原厂线路保持未注册。Web声音、应用两组读取和故障索引回归通过，没有改账号或发起通话、短信、网络切换。
- 私有原件均在`research/d31/captures/2026-09-11/parallel-batch6-*`下，沿用本批目录日期；零时后的结果在本节明确实际日期。主要索引为`parallel-batch6-discovery-web114/result.json`、`parallel-batch6-media-web114/result.json`和`parallel-batch6-runtime114/result.json`。原失败捕获和112更正前统计保留。

## 收尾与后续边界

- 本批完成有界故障发现、宿主可恢复补传及真实APP媒体准备。队列两事件真Web取回/验包/归档已在110完成，114不重复上传它们；宿主队列仍按详细`index 1`取事件，新`pending`用于轻量管理查询，未声称已接成设备开机自动上传。
- 后续媒体先解决自身录音归属与电话占用竞态，再做有界单向会话及退出验收。当前占用快照不是音频资源原子预留；自身采集也可能被全局活动检查否决。告警、双向音视频和D31丢失模式尚未验收。
- Web需按具体媒体模式声明和校验能力，并接入故障分页、缺口及归档阶段；精确交接由D31_dev追加至`remote.md`，本批不修改共享Web。
- 基础探针继续约133KiB，完整客户端约4.4MiB。Windows候选、正式固件及下载链接不变；首次基础转系统完整、完整实刷、双机比较和业务修复效果仍按既定条件延期。

- 328项本批私有捕获已逐项登记路径、长度、SHA-256和文件时间，共18779899字节，索引位于`parallel-batch6-evidence-index/artifacts-private.json`。原始信息和会话不进入公开仓库。四位agent已完成专项结果复核与公共媒体接线审查；没有发现阻断本批准备/查询范围的问题。
- 已同步阅读`remote.md`最新D22丢失模式异常路径复核；D31后续须跟踪修正后的合同，不能从169正常流程通过推断时间异常、退出中断等场景已完成。本批未执行锁屏或清除。
