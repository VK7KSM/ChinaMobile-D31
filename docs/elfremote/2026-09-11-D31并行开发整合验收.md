# 2026-09-11 D31并行开发整合验收

## 本轮目标和职责

用户要求记录完整开发计划，并由多个代理同时开发独立模块，总负责人统一整合、验收和提交。计划已写入主实施计划第十二节及D31.md第38.390节。首批实际启动双制品构建、文件协议、离线诊断比较和剩余业务审计；审计完成后，该代理接着实现Windows急救ADB端口兼容。

既有云连接、自更新、离线APK核心交接、文件传输、ADB和账号配置沿用已验收成果。新增诊断没有替代其它工作；Windows和完整业务按依赖继续推进。当前不修改共享Web、原独立系统支持或启动入口。

## 总负责人整合内容

- 完整客户端文件能力新增真实删除声明，与本轮FileOperations实现一起交付；未安装的新版本不代表线上旧设备已有此能力。
- 完整云更新在APK下载并核验后检查完整制品标记，防止同包同签名的基础救援包进入完整客户端更新。元数据工具从实际APK读取标记，发布准备拒绝基础类型；手动完整APK交接继续复用85版逻辑。
- 原完整候选构建和诊断构建工具适配fullRelease，诊断构建可以明确使用本次独立编译目录，避免误取历史类文件。
- 新增离线比较命令行工具DiagnosticCompareMain；它只处理已获取的清单和规则，输出新报告，不采集设备或执行修复。输入有大小限制，已有报告不覆盖。
- 新增D31隔离文件验收工具RemoteFileProtocolCheck，计划在全新合成目录验证真实权限、覆盖原像保存、目录和坏链接删除不越界。工具编写完成不等于已在设备执行。

## 已发生的构建记录

1. 首次编译证据在`research/d31/staging/parallel-first-compile-20260911/`。错误是选择的旧Gradle缓存缺少MQTT和WebSocket依赖；没有更改依赖版本或从其它APK冒取内容。
2. 使用已有完整缓存后，`parallel-compile-cache-fixed-20260911/`中的basic/full Java组合编译通过。此时仍为开发中编译，不是冻结候选验收。
3. 首轮双制品测试在`parallel-candidates-20260911/`失败：基础测试26项中1项失败。原始JUnit结果确认锁住数据文件后再次打开文件计算摘要，在Windows触发共享锁错误。交回原代理修复，不跳过该测试、不将失败包装成通过，原输出保留。

## 验收与提交边界

- 各任务的源码完成、离线通过、Android真实行为通过、Web通过和正式发布分别记录。第一轮没有全量系统采集、双机诊断或远程修复验收，比较核心通过不能冒充整机一致性功能已经交付。
- 基础APK至完整系统APK的真实接替、系统/data更新副本优先级、无网冷启动、电话/存储等综合回归仍须后续独立验收。当前不把新的基础APK覆盖安装到已有完整系统组件上进行试错。
- 统一提交使用ChinaMobile-D31实际Git仓库的独立开发分支，基于已核对的origin/main。只同步明确源码和中文开发文档；公开源代码不包含签名私钥、凭据、原始日志或设备私人状态。正式下载链接、APK、EXE及固件发布不因源码提交自动更新。

后续实际测试、失败修正、制品摘要及提交结果在本记录继续追加。

## D31隔离文件验证预登记

- 目标仅为当前D31，Android 6/API23，设备构建以本次只读基线原文记录。使用电脑5042与已确认的完整D31序列号，不操作其它ADB服务器。
- 假设：两种候选的真实文件操作能在API23运行；完整候选可读取真实权限、保留覆盖原像、生成/核验文件快照，并删除目录及坏链接而不越过链接；基础候选可使用真实文件锁、分块重试和硬链接提交。
- 候选来源为`parallel-candidates-final-20260911/build/outputs/apk/`。完整92版398682字节，SHA-256为`c9c5593b71afed3ee01a3cf536fa10a30907bf15099a033eb5827bfc6c5559a7`；基础91版135651字节，SHA-256为`c2a3678722f71ed4661dd6133f2cd38770f66f57fd6f3bd5177bfb903eb74168`。两者原签名及实际完整类型标记已分别读取验证。此轮组合lint仍有源码目录模型问题，不能称为完整构建验收通过；本项仅验证已编译候选的隔离文件行为。
- 完整测试JAR为`7c2ab95f63cdb6c9374348a340bf3b7ef46b9ac436c04d438bf5aac7fd6df63b`，基础测试JAR为`8e0168199215b898e96ce6a54d503d8d0813d0daf607a96780782da1f65e3d92`。源码与编译记录保存在`research/d31/captures/2026-09-11/parallel-file-protocols/`。
- 操作为向全新`/data/local/tmp/d31-parallel-check-20260911`目录推送两包和测试JAR；分别用`CLASSPATH=<候选APK>:<测试JAR> /system/bin/app_process /system/bin net.elfradio.d31bootstrap.RemoteFileProtocolCheck /data/local/tmp/d31-file-protocol-20260911`和同样方式的`BasicFileProtocolCheck /data/local/tmp/d31-basic-protocol-20260911`执行。精确完整命令、时间、序列号及构建原文保存在私有命令记录。
- 不通过PackageManager安装，不更换活动核心、不运行APK界面/服务、不修改系统或启动入口。仅测试目录中的合成文件会创建、改名、复制或删除；测试原件及结果保留并拉回，不读取用户文件，不进行通话、短信、重启或刷写。
- 通过条件为两个工具明确成功、结果JSON符合预期、非目标哨兵文件哈希保持、安装版本及既有维护状态保持。失败立即保存原始输出与目录，不重放相同任务覆盖现场；因未修改原系统文件无需系统回滚，测试目录先保留作证据。

## 隔离验证结果与构建修正

- D31实际构建为`alps/full_hct6737t_66_m0/hct6737t_66_m0:6.0/MRA58K/1583081804:userdebug/test-keys`。两项独立进程分别返回`FILE_PROTOCOL_CHECK_OK`、`BASIC_FILE_PROTOCOL_CHECK_OK`，结果JSON均为`passed=true`。完整测试覆盖真实权限、覆盖备份、文件快照及恢复、目录链接与坏链接删除；基础测试覆盖真实文件锁、分块重试、整文件哈希拒绝、硬链接提交及已有目标保留。
- 两套合成目录、结果和原始命令输出均拉回`research/d31/captures/2026-09-11/parallel-file-protocols/`并生成完整清单。只删除测试代码自己创建的待删除目录/链接；哨兵和原件保持，不包含用户文件操作。
- 结束时已安装客户端仍为90版`1.18.4-sip-config`，系统应用更新身份保持，adbd及原8765正常。包管理器的隐藏系统记录与活动应用记录各自保留，不据单个版本字段宣称固定系统载荷完全一致；后续全量采集必须分别核对真实APK、隐藏登记和运行核心。
- 上述证明候选的实际Android文件行为，不代表基础APK蓝牙安装、基础到系统完整版接替、FAT/exFAT各卷、Web新协议、冷启动或综合电话业务已通过。
- 第三轮完整组合仍遇到同一基础lint错误。实际lint模型显示，Java目录已隔离，但Kotlin默认源码目录再次带入`src/main/java`；保留`parallel-candidates-final-20260911/`失败报告，继续修正真实模型，未加多余权限或压制错误。两次源码目录修正是构建范围问题，不是用忽略错误替代设备修复。
- 离线比较CLI四项通过：范围内匹配、缺失证据明确不足、未知合同生成未比较报告、已有报告不覆盖。证据为`research/d31/staging/parallel-diagnostics-cli-20260911/`；这些合成清单不是实际整机采集。
- Windows149项端口及停止时序检查、原24项急救协议检查通过。恢复命令为199字节，保留停止后1秒等待；此为宿主命令替身和协议验证，未执行真实uptool恢复。

## 最终组合构建及证据对应

- 最终构建目录为`research/d31/staging/parallel-candidates-verified-20260911/`。Gradle的108项任务完成，基础28项、完整236项单元测试均无失败或错误；完整测试的5项Windows链接权限受限用例跳过，其它259项通过。上述D31独立进程测试补充真实链接行为，但不声称覆盖所有外部卷和并发条件。
- 基础和完整Lint均为零错误，分别保留3和28项警告。警告包括已有Android内部接口、API兼容、资源及文本建议；没有关闭生成代码检查。TLS警告对应补充公开根证书的验证器，实际仍调用证书链验证，不是信任所有证书的实现。
- 最终基础APK为91版、135651字节，SHA-256为`020668562a317ddc0c4f3dd27e8e2f08c7ffe4a4ea3b034f5ad699c9439f45aa`；完整APK为92版、398682字节，SHA-256为`a3b9540588677ceed6c6393b20febb3b43ed40e65682ff575b94ba8a7f33b4c8`。版本仍带候选标记，不是新的正式发布。
- 与已完成D31隔离测试的上一轮两包逐个归档条目比较，基础18项、完整39项均内容一致，DEX摘要也一致。APK整体摘要不同，不能把两份APK说成同一个文件；`已测候选内容对照.json`记录双方摘要与实际内容对应关系，避免无依据地重复设备测试。
- 真实Lint模型已不包含原始main/test目录，生成副本与原件101项哈希一致。构建完成后模型验包脚本仍报告路径集合不匹配，作为独立验包缺陷保留，修正及最终验包结果继续追加；不把Gradle成功单独当作整体验包通过。

## 下一批可并行启动的工作

1. 双制品实际交接及Windows内置最小APK适配，与完整客户端业务独立推进。先检查系统预置、data更新副本和活动核心的实际来源，不能只靠versionCode推断接替成功。
2. 基于已定比较合同，分别实现有界设备清单/故障采集、固件预期清单生成、修复事务状态机；三个模块可同时开发，实际修复执行等适用条件及回滚验收完成。
3. 文件能力由总负责人安排现有Web实际联调；系统/网络等剩余适配依照主计划拆分独占模块。必须改Web的具体需求先告知用户并写入协作文档，当前没有修改共享Web。
4. 双机、实刷、首次离网启动和业务综合验收仍是最终交付条件。首批源码提交不会升级开发板、更新正式下载或宣称朋友设备已修复。

## 最终验包通过

- 模型验包失败的根因是PowerShell单元素数组解包为字符串，导致目录追加成为字符串拼接。修复校验脚本的集合与路径规范化，保留原失败结果，没有改动APK运行源码或重新制造候选。
- 总负责人运行`Build-RemoteVariants.ps1 -Mode Verify`成功。四份真实模型分别核对基础主源码16项、基础测试源码6项、完整主源码65项、完整测试源码36项；生成源码检查保持开启，101项受控副本逐项匹配。
- 同包身份、原证书、91至92版本顺序、唯一制品标记、实际DEX功能与运行依赖全部通过，候选索引为上述最终目录的`remote-variants.json`。基础只含NanoHTTPD运行依赖，完整保留原云连接及更新等实现。
- 至此首批组合达到源码和离线候选验收；真实D31隔离文件行为通过。尚未执行正式安装、基础到系统完整版接替、实际Web新文件流程、冷启动或实刷，不能将本状态推广为这些环节通过。

## 公开源码复核

- 公开目录再次独立执行基础与完整Java编译，33项任务通过，证据保存在`research/d31/staging/parallel-published-compile-20260911/`。此项验证同步后的源码完整性，不是重复安装或新的设备验收。
- 本批同步188项客户端源码、Windows修改及计划/验收文件；来源清单同时记录冻结原件SHA256和按Git属性规范化后的文件对象。暂存范围仅为客户端源码、Windows本批源码和`docs/elfremote`，没有包含APK、私钥或原始设备捕获。
- 仓库原有`systemSupport`仍为旧基线，其版本配置、通知注册及新增通知类与本地新版存在差异。本批`app`不依赖该模块编译，也未引用其新增通知类，因此不阻断本批双制品源码；本次不把独立系统支持列为已同步、升级或验收。后续固件整合必须另外纳入已经验证的最新版系统支持，不能拿本分支所有目录直接重制固件。
- Windows配套源码未发现缺失依赖，新增测试不由历史构建脚本自动运行。重复验证须使用公开源码已有的`BuildConstants.g.cs`；既有研究目录生成文件和旧版真机脚本不能当作新版EXE验收依据。

## 宿主工具公开整理完成

- 最终审查发现10份历史验收工具仍包含固定设备端点或设备标签，部分工具静态导入另一个本地Web项目的文件。这些值和路径不能作为公开工具的默认目标或必要隐含依赖。
- 保留原件后将目标改为显式参数；两个PowerShell工具使用`-Serial`，七个云端Node工具在原参数末尾追加`deviceName`，原始ADB协议检查显式指定主机和端口。缺少目标在设备和网络动作前拒绝。
- 会话校验提取为工具目录内的纯函数，保持原域名、有效期及Cookie字符检查。两个WebSocket工具按需加载声明的`ws`依赖；增加`tools/package.json`，不复制Web端源码、凭据或依赖目录。APP运行代码、APK和Web均未改动。
- 总负责人在新的隔离副本复核并落盘：2项PowerShell语法、9项Node语法、21项缺参/非法参数拒绝、10项显式参数离线检查、6项会话检查均通过，真实网络请求0次。原始输出和网络拦截证据位于`research/d31/staging/parallel-public-tools-verified-20260911/`，最终12文件摘要单独保存。两个WebSocket工具的显式参数检查仅验证缺依赖提示，未安装依赖或冒充真实WebSocket通信验收。
- 增加这两份工具文件后，最终来源清单共190项。再次检查未残留固定开发板端点、设备标签或相邻Web项目导入；公开源码不含原私有脚本备份。
