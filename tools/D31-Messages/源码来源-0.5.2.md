# 2026-09-13 短信0.5.2源码来源

## 冻结来源

上游为 [QUIK](https://github.com/quik-sms/quik)，提交 `555b8822c654b8ee85bb9d3f961eb30f232b1079`。354项自定义覆盖文件来自 `research/d31_phone_sms/source/`，已与短信0.5.2隔离测试及构建使用的冻结源码逐字核对。权威版本配置已同步为代码11、`0.5.2-dev`，debug变种实际为 `0.5.2-dev-debug`。

`source-files.json`只使用相对来源路径，逐项登记文件长度和SHA-256。其中还登记本次补充的第三方许可文本；这些许可文本不参与APK逻辑。`version-11.patch`是代码10到11、0.5.1到0.5.2的原始两行版本补丁，覆盖层内 `presentation/build.gradle`已包含，不应再次应用。

未同步任何真实账号、短信数据库、设备标识、签名文件、口令、日志、旧制品、构建目录或机器配置。SIP配置中的字段名与空默认值是代码合同，不是实际账号。`cacert.pem`为公开CA证书集合，不是私钥。

## 还原及验证

1. 获取上游仓库，切换到上述完整提交。
2. 将 `overlay` 的内容按相同相对路径覆盖上游，保留上游其余文件及许可证。本次覆盖层没有要求删除的上游文件。
3. 配置JDK17、Android SDK34，使用上游Gradle8.2包装器；首次构建需取得依赖。

在还原后的项目根目录运行：

```powershell
./gradlew.bat :presentation:assembleDebug
./gradlew.bat :presentation:testDebugUnitTest -ProbolectricDependencyDir=本机已缓存的Android9运行库目录 --tests dev.octoshrimpy.quik.worker.InjectionWorkerFactoryTest --tests dev.octoshrimpy.quik.feature.phone.D31NotificationsTest --tests dev.octoshrimpy.quik.feature.phone.D31UnreadProviderTest --tests dev.octoshrimpy.quik.feature.phone.SipUriTest
```

正式构建保留 `robolectric.offline=true`，因此专项测试需要事先取得Robolectric Android9运行库 `org.robolectric:android-all-instrumented:9-robolectric-4913185-2-i4`，并将该目录作为上方参数。缓存完整时可另加 `--offline`。测试名称使用真实包名，不按源码目录名称猜测。

冻结来源已完成32项专项测试，0失败、0错误、0跳过：WorkerFactory 6项、通知13项、未读Provider 3项、SIP地址10项。冻结APK另已核对实际直接构造Worker的字节码。本次同步只做文件、来源及公开范围核对，没有在公开目录重新运行Gradle，也没有设备安装或短信业务实测。

## 签名及原生库边界

冻结APK为 `net.elfradio.d31phone.debug`，代码11、`0.5.2-dev-debug`，69777875字节，SHA-256为 `49300FC626B5493F490013414CBCD0B9FB8DCA31729B2E36DA1328906D75F6EF`。该APK只用于来源核对，本目录不附带APK、签名私钥或本地签名配置。默认debug构建使用构建者本机证书，不能保证覆盖正式制品；源码相同也不代表APK哈希相同。

覆盖层保留原有 `arm64-v8a` 的 `libpjsua2.so`、`libopenh264.so`、`libc++_shared.so`及PJSUA2 Java绑定，三份原生库均与既有网关副本逐字一致。本轮没有重编这些库。Realm原生库由Gradle依赖提供，不是额外加入覆盖层的制品。

既有网关构建配置声明PJSIP 2.14.1、OpenH264 `openh264v2.1.0`及NDK r21e；这只是脚本来源线索，尚不能证明三份既有二进制的完整构建配置或逐字可复现性。本目录不含原生依赖全量源码、完整工具链或OAT生成流程，不宣称仅靠本覆盖层就能从源码重建全部原生库或固件。

## 许可来源

QUIK及其定制代码继续保留目录中的GPLv3许可证及原版权头。第三方库各自许可独立保留，不改为仓库根MIT许可。

- `overlay/third-party-licenses/PJSIP-COPYING.txt`取自PJSIP 2.14.1的[官方COPYING](https://github.com/pjsip/pjproject/blob/2.14.1/COPYING)。
- `overlay/third-party-licenses/OpenH264-LICENSE.txt`取自OpenH264 `openh264v2.1.0`的[官方LICENSE](https://github.com/cisco/openh264/blob/openh264v2.1.0/LICENSE)。
- `overlay/third-party-licenses/libcxx-LICENSE.txt`为LLVM 9.0.1的[官方libc++许可原文](https://github.com/llvm/llvm-project/blob/llvmorg-9.0.1/libcxx/LICENSE.TXT)，保留Apache-2.0及LLVM例外和历史许可部分。此文本是上游许可参考，不是对既有库准确编译版本的证明。

准确原生库版本及完整对应源码仍存在上述复现缺口，不能仅凭许可文本和文件哈希宣称已经补齐。
